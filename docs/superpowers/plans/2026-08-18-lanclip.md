# lanclip Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Общий буфер обмена между Mac и Windows-ПК по локальной сети — текст, картинки и файлы, вставка содержимого буфера соседней машины по глобальному Ctrl+Shift+V.

**Architecture:** Два симметричных резидентных агента, по одному на машину. Каждый одновременно сервер (отдаёт свой буфер по HTTP/JSON на порту 8899) и клиент (по хоткею тянет буфер соседа, наполняет локальный буфер и синтезирует нажатие паста). Ничего не уходит в сеть, пока не нажат хоткей. Ядро логики (манифест, конфиг, HTTP, стейджинг, резолв соседа, pull) отделено от платформенного доступа к буферу интерфейсами, поэтому весь протокол тестируется с подставным буфером без GUI.

**Tech Stack:** macOS — Swift 6.3, SwiftPM, `Network.framework` (NWListener/NWConnection), Carbon `RegisterEventHotKey`, `CGEvent`, XCTest. Windows — C# 5.0, компиляция `csc.exe` из .NET Framework 4.8, `HttpListener`, `System.Windows.Forms.Clipboard`, `RegisterHotKey`/`SendInput`, самописный мини-раннер тестов. Ноль сторонних зависимостей на обеих сторонах.

**Spec:** `docs/superpowers/specs/2026-08-18-lanclip-design.md`

## Global Constraints

- **C# только 5.0.** На ПК `csc.exe` версии 4.8.9232.0 — последний не-Roslyn компилятор. Запрещены: интерполяция строк `$"..."`, null-conditional `?.` и `?[]`, `nameof`, expression-bodied члены (`=>`), инициализаторы автосвойств, кортежи, pattern matching, локальные функции, `out var`, `using static`. Разрешены: LINQ, `async`/`await`, генерики, лямбды, `var`, `dynamic`.
- **Ноль сторонних зависимостей.** На ПК нет .NET SDK и нет NuGet — только рантаймы; ссылки допускаются исключительно на DLL из `C:\Windows\Microsoft.NET\Framework64\v4.0.30319` (проверено: `System.Windows.Forms.dll`, `System.Drawing.dll`, `System.Web.Extensions.dll`, `System.Runtime.Serialization.dll` на месте). На Mac — ни одного пакета в `Package.swift`.
- **Никакого URLSession на Mac.** ATS блокирует plain-HTTP из процессов без Info.plist. Клиент реализуется на том же `NWConnection`, что и сервер, — один HTTP-кодпат на обе роли.
- Порт по умолчанию `8899`, заголовок авторизации `X-Clip-Token`, JSON в UTF-8.
- Конфиг: Mac `~/.config/lanclip/config.json`, Windows `%USERPROFILE%\.config\lanclip\config.json`. Формат: `{"port":8899,"token":"<32 hex>","peers":[…],"maxBytes":536870912,"autoPaste":true}`.
- Стейджинг: Mac `~/Library/Application Support/lanclip/incoming/<stamp>/`, Windows `%LOCALAPPDATA%\lanclip\incoming\<stamp>\`. `stamp` — `yyyyMMdd-HHmmss`. Уборка: партии старше **7 дней** и всё за пределами **20 последних** партий.
- `maxBytes` по умолчанию `536870912` (512 МБ).
- `seq` в манифесте — счётчик изменений буфера ОС: `NSPasteboard.general.changeCount` на Mac, `GetClipboardSequenceNumber()` на Windows.
- **Адреса:** Mac `192.168.1.102`, mDNS-имя `cab-wsm-0667615.local`; ПК `192.168.1.184`, в `/etc/hosts` Mac как `pc`, имя машины `DESKTOP-NGNE28E`, учётка `paulislava`, домашняя папка `C:\Users\PaulIsLava`. С ПК mDNS-имя Mac резолвится, IPv4 в обе стороны ходит.
- **Работа с ПК:** оболочка по SSH — PowerShell 7.6.5, сессия уже с правами администратора. Код доставляется `scripts/sync-win.sh` (через `scp`, проверено). Процессы, запущенные из SSH-сессии, умирают вместе с ней — долгоживущее только через `Register-ScheduledTask`.
- **PowerShell пишет BOM** при `Set-Content -Encoding utf8`, что ломает JSON-конфиг. Внутри скриптов на ПК писать только через `[IO.File]::WriteAllText($p,$t,(New-Object System.Text.UTF8Encoding($false)))`.
- Коммиты на русском, без тегов `Co-Authored-By`, с завершающей строкой `Claude-Session: https://claude.ai/code/session_01BgCFcN67viuABFatKQNBB2`.

## Разрешения, которые нельзя забыть

Три вещи требуют разовой выдачи прав и обязаны быть проверены до объявления работы готовой:

1. **macOS Accessibility** — синтез нажатия через `CGEvent` без него молча не работает (сам `RegisterEventHotKey` разрешений не требует).
2. **macOS Local Network** — на macOS 15+ доступ к локальной сети требует разрешения; фоновому LaunchAgent диалог может не показаться, поэтому первый запуск делается из Терминала руками.
3. **Windows** — `netsh http add urlacl` (иначе неэлевированная задача не сможет слушать `http://+:8899/`) и правило файрвола на порт 8899.

## File Structure

```
lanclip/
├─ proto/PROTOCOL.md                    контракт между сторонами, единый источник истины
├─ mac/
│  ├─ Package.swift
│  ├─ Sources/LanClipCore/              платформенно-независимое ядро
│  │   ├─ RelPath.swift                 нормализация относительных путей
│  │   ├─ Manifest.swift                модель манифеста + JSON
│  │   ├─ Config.swift                  чтение, валидация, генерация токена
│  │   ├─ Clipboard.swift               ClipContent + протоколы чтения/записи
│  │   ├─ Snapshot.swift                снимок буфера, обход папок, доступ к блобам по seq
│  │   ├─ Net.swift                     проверка приватности адреса
│  │   ├─ HttpMessage.swift             парсер и сериализатор HTTP/1.1
│  │   ├─ HttpServer.swift              NWListener, маршруты, авторизация
│  │   ├─ HttpClient.swift              NWConnection-клиент: manifest/blob/health
│  │   ├─ Staging.swift                 партии принятых файлов и уборка
│  │   ├─ PeerResolver.swift            перебор peers, кеш живого адреса
│  │   ├─ PullClient.swift              полный цикл pull
│  │   └─ Notifier.swift                протокол уведомлений + no-op
│  ├─ Sources/lanclipd/                 платформенная часть и CLI
│  │   ├─ main.swift                    serve / status / get / pull
│  │   ├─ MacPasteboard.swift           NSPasteboard
│  │   ├─ MacHotkey.swift               RegisterEventHotKey + CGEvent
│  │   └─ MacNotifier.swift             display notification
│  └─ Tests/LanClipCoreTests/
├─ win/
│  ├─ src/                              зеркало ядра + Win-специфика
│  ├─ tests/                            мини-раннер и тесты
│  └─ build.ps1                         сборка обоих exe через csc
├─ scripts/
│  ├─ gen-token.sh                      генерация общего токена
│  ├─ sync-win.sh                       доставка win/ на ПК и запуск сборки
│  ├─ install-mac.sh                    сборка, установка, LaunchAgent, ad-hoc подпись
│  └─ install-win.ps1                   urlacl, файрвол, Register-ScheduledTask
├─ ai/{PROJECT,FEATURES,ERRORS}.md
└─ docs/superpowers/{specs,plans}/
```

Ядро (`LanClipCore` / `win/src` без `Win*`-файлов) не знает про платформенный буфер: `HttpServer` и `PullClient` работают через `ClipboardReading`/`ClipboardWriting`. Это граница, которая делает протокол тестируемым без GUI, и её нельзя размывать.

---

### Task 1: Скелет проектов, протокол и оба тест-раннера

Задача существует, чтобы обе сборки и оба тест-раннера были зелёными до того, как появится хоть одна строчка логики. Тулчейн уже проверен вручную; эта задача фиксирует проверку в коде.

**Files:**
- Create: `proto/PROTOCOL.md`
- Create: `.gitignore`
- Create: `mac/Package.swift`
- Create: `mac/Sources/LanClipCore/Version.swift`
- Create: `mac/Sources/lanclipd/main.swift`
- Test: `mac/Tests/LanClipCoreTests/VersionTests.swift`
- Create: `win/src/Version.cs`
- Create: `win/tests/TestRunner.cs`
- Create: `win/tests/VersionTests.cs`
- Create: `win/build.ps1`
- Create: `scripts/sync-win.sh`

**Interfaces:**
- Consumes: ничего.
- Produces: `LanClipCore.protocolVersion: Int` (Swift) и `LanClip.Version.Protocol` (C#), оба `1`; мини-фреймворк `T` с методами `T.Eq<TV>(TV expected, TV actual, string name)`, `T.True(bool cond, string name)`, `T.Throws<TE>(Action body, string name)`, `T.Run(string name, Action body)`, `T.Summary() -> int`; `scripts/sync-win.sh` — доставка `win/` на ПК и запуск сборки с тестами.

- [ ] **Step 1: Написать PROTOCOL.md**

Файл — контракт, по которому обе реализации обязаны совпадать. Содержимое:

````markdown
# lanclip protocol v1

HTTP/1.1, порт по умолчанию 8899. Авторизация: заголовок `X-Clip-Token`.
Ответ `401` без тела на неверный или отсутствующий токен. Ответ `403` без тела,
если адрес источника не принадлежит приватному диапазону.

`seq` — счётчик изменений буфера ОС (`NSPasteboard.changeCount`,
`GetClipboardSequenceNumber`). Клиент возвращает его при загрузке блобов; это
единственный механизм обнаружения того, что буфер уехал посреди передачи.

## GET /health

200: `{"ok":true,"host":"<hostname>","version":1}`

## GET /clip

Порядок определения типа: файлы → картинка → текст. Первое совпадение выигрывает.

```json
{"kind":"empty","seq":41}
{"kind":"text","seq":42,"text":"…"}
{"kind":"image","seq":43,"totalSize":48213,
 "blobs":[{"i":0,"rel":"clip.png","size":48213,"mime":"image/png"}]}
{"kind":"files","seq":44,"totalSize":96354,
 "blobs":[{"i":0,"rel":"отчёт.pdf","size":91234},
          {"i":1,"rel":"img/a.png","size":5120}]}
```

- `text` присутствует только при `kind:"text"`.
- `blobs` и `totalSize` присутствуют при `kind:"image"` и `kind:"files"`,
  отсутствуют при `text` и `empty`. `totalSize` — сумма `size` всех блобов.
- Картинка всегда кодируется в PNG независимо от исходного формата в буфере,
  `rel` для неё всегда `clip.png`.
- Папки обходятся рекурсивно, `rel` сохраняет структуру и всегда использует
  прямые слеши. Пустые папки в манифест не попадают.

## GET /clip/blob/{i}?seq={seq}

200 `application/octet-stream`, сырые байты, стримом, с `Content-Length`.
`409` без тела, если текущий счётчик изменений не равен `seq`.
`404` без тела, если индекс вне диапазона манифеста.

Сервер держит снимок последнего отданного манифеста вместе с его `seq`, чтобы не
пересчитывать блобы на каждый запрос: для текста — саму строку, для картинки —
PNG-байты, для файлов — список абсолютных путей.

## POST /pull

Агент выполняет цикл «резолв соседа → `/clip` → загрузка → запись в локальный
буфер» и отвечает `{"kind":…,"fileCount":N,"bytes":N}`. Пасты не синтезирует.
`503 {"error":"<текст>"}`, если сосед недоступен или передача сорвалась.
````

- [ ] **Step 2: Написать .gitignore**

```gitignore
.build/
*.o
*.exe
*.pdb
.DS_Store
```

- [ ] **Step 3: Написать Package.swift**

```swift
// swift-tools-version:6.0
import PackageDescription

let package = Package(
    name: "lanclip",
    platforms: [.macOS(.v14)],
    targets: [
        .target(name: "LanClipCore"),
        .executableTarget(name: "lanclipd", dependencies: ["LanClipCore"]),
        .testTarget(name: "LanClipCoreTests", dependencies: ["LanClipCore"]),
    ]
)
```

- [ ] **Step 4: Написать падающий тест версии протокола**

`mac/Tests/LanClipCoreTests/VersionTests.swift`:

```swift
import XCTest
@testable import LanClipCore

final class VersionTests: XCTestCase {
    func testProtocolVersionIsOne() {
        XCTAssertEqual(protocolVersion, 1)
    }
}
```

- [ ] **Step 5: Убедиться, что тест не собирается**

Run: `cd mac && swift test`
Expected: FAIL — `cannot find 'protocolVersion' in scope`.

- [ ] **Step 6: Реализовать минимум**

`mac/Sources/LanClipCore/Version.swift`:

```swift
public let protocolVersion = 1
```

`mac/Sources/lanclipd/main.swift`:

```swift
import LanClipCore

print("lanclip protocol v\(protocolVersion)")
```

- [ ] **Step 7: Проверить, что тест проходит**

Run: `cd mac && swift test`
Expected: PASS, 1 тест.

- [ ] **Step 8: Написать мини-раннер тестов для Windows**

`win/tests/TestRunner.cs`. Учесть запрет C# 6+: никакой интерполяции строк, никаких expression-bodied членов.

```csharp
using System;
using System.Collections.Generic;

namespace LanClip.Tests
{
    static class T
    {
        static int passed = 0;
        static readonly List<string> failures = new List<string>();

        public static void Run(string name, Action body)
        {
            try
            {
                body();
            }
            catch (Exception e)
            {
                failures.Add(name + " threw " + e.GetType().Name + ": " + e.Message);
            }
        }

        public static void Eq<TV>(TV expected, TV actual, string name)
        {
            if (!object.Equals(expected, actual))
            {
                failures.Add(name + ": expected <" + Show(expected) + ">, got <" + Show(actual) + ">");
            }
            else
            {
                passed++;
            }
        }

        public static void True(bool cond, string name)
        {
            if (!cond) { failures.Add(name + ": expected true"); } else { passed++; }
        }

        public static void Throws<TE>(Action body, string name) where TE : Exception
        {
            try
            {
                body();
                failures.Add(name + ": expected " + typeof(TE).Name + ", nothing thrown");
            }
            catch (TE)
            {
                passed++;
            }
            catch (Exception e)
            {
                failures.Add(name + ": expected " + typeof(TE).Name + ", got " + e.GetType().Name);
            }
        }

        static string Show(object v)
        {
            if (v == null) { return "null"; }
            return v.ToString();
        }

        public static int Summary()
        {
            foreach (string f in failures) { Console.WriteLine("FAIL " + f); }
            Console.WriteLine("passed=" + passed + " failed=" + failures.Count);
            return failures.Count == 0 ? 0 : 1;
        }
    }
}
```

- [ ] **Step 9: Написать падающий тест версии на C#**

`win/tests/VersionTests.cs`:

```csharp
using System;

namespace LanClip.Tests
{
    static class VersionTests
    {
        public static void Register()
        {
            T.Run("protocol version is 1", delegate
            {
                T.Eq(1, LanClip.Version.Protocol, "protocol");
            });
        }
    }

    static class Program
    {
        static int Main()
        {
            VersionTests.Register();
            return T.Summary();
        }
    }
}
```

- [ ] **Step 10: Написать build.ps1**

`win/build.ps1` собирает агент и тесты одним компилятором и запускает тесты.

```powershell
$ErrorActionPreference = "Stop"
$fw   = "C:\Windows\Microsoft.NET\Framework64\v4.0.30319"
$csc  = Join-Path $fw "csc.exe"
$root = Split-Path -Parent $MyInvocation.MyCommand.Path
$out  = Join-Path $root "out"
New-Item -ItemType Directory -Force -Path $out | Out-Null

$refs = @("System.dll","System.Core.dll","System.Windows.Forms.dll","System.Drawing.dll",
          "System.Web.Extensions.dll","System.Runtime.Serialization.dll") |
        ForEach-Object { "/r:" + (Join-Path $fw $_) }

$src   = Get-ChildItem (Join-Path $root "src")   -Filter *.cs | ForEach-Object { $_.FullName }
$tests = Get-ChildItem (Join-Path $root "tests") -Filter *.cs | ForEach-Object { $_.FullName }

# Агент: WinExe, чтобы не мигало консольное окно у резидентного процесса.
& $csc /nologo /warnaserror- /target:exe /out:"$out\lanclipd.exe" $refs $src
if ($LASTEXITCODE -ne 0) { throw "сборка агента упала: $LASTEXITCODE" }

# Тесты: агент без Program.cs плюс тестовые файлы.
$srcNoMain = $src | Where-Object { $_ -notmatch "Program\.cs$" }
& $csc /nologo /warnaserror- /target:exe /out:"$out\lanclip-tests.exe" $refs $srcNoMain $tests
if ($LASTEXITCODE -ne 0) { throw "сборка тестов упала: $LASTEXITCODE" }

& "$out\lanclip-tests.exe"
exit $LASTEXITCODE
```

- [ ] **Step 11: Написать sync-win.sh**

`scripts/sync-win.sh` — доставка и прогон в одну команду. Дальше каждая Windows-задача пользуется только им.

```bash
#!/usr/bin/env bash
set -euo pipefail

PEER="${LANCLIP_PEER:-paulislava@pc}"
DEST='C:/Users/PaulIsLava/lanclip'
HERE="$(cd "$(dirname "$0")/.." && pwd)"

ssh "$PEER" "New-Item -ItemType Directory -Force -Path '$DEST\\win\\src','$DEST\\win\\tests' | Out-Null" >/dev/null

# scp не удаляет исчезнувшие файлы — чистим целевые папки перед копированием.
ssh "$PEER" "Remove-Item '$DEST\\win\\src\\*.cs','$DEST\\win\\tests\\*.cs' -ErrorAction SilentlyContinue" >/dev/null

scp -q "$HERE"/win/src/*.cs   "$PEER:$DEST/win/src/"
scp -q "$HERE"/win/tests/*.cs "$PEER:$DEST/win/tests/"
scp -q "$HERE"/win/build.ps1  "$PEER:$DEST/win/"

ssh "$PEER" "& '$DEST\\win\\build.ps1'"
```

- [ ] **Step 12: Проверить, что C#-тесты падают**

Run: `chmod +x scripts/sync-win.sh && ./scripts/sync-win.sh`
Expected: сборка агента падает — `Version` не существует.

- [ ] **Step 13: Реализовать версию на C#**

`win/src/Version.cs`:

```csharp
namespace LanClip
{
    static class Version
    {
        public const int Protocol = 1;
    }
}
```

`win/src/Program.cs`:

```csharp
using System;

namespace LanClip
{
    static class Program
    {
        static int Main(string[] args)
        {
            Console.WriteLine("lanclip protocol v" + Version.Protocol);
            return 0;
        }
    }
}
```

- [ ] **Step 14: Проверить, что оба раннера зелёные**

Run: `cd mac && swift test && cd .. && ./scripts/sync-win.sh`
Expected: Swift — 1 тест PASS; Windows — `passed=1 failed=0`, код выхода 0.

- [ ] **Step 15: Коммит**

```bash
git add proto .gitignore mac win scripts
git commit -m "feat: скелет проектов lanclip, протокол v1 и тест-раннеры обеих платформ"
```

---

### Task 2: Mac — нормализация относительных путей

Первая содержательная единица ядра, и одновременно граница безопасности: `rel` приезжает с чужой машины и превращается в путь на диске.

**Files:**
- Create: `mac/Sources/LanClipCore/RelPath.swift`
- Test: `mac/Tests/LanClipCoreTests/RelPathTests.swift`

**Interfaces:**
- Consumes: ничего.
- Produces: `public enum RelPath { public static func normalize(_ raw: String) -> String? }` — возвращает безопасный относительный путь с прямыми слешами либо `nil`, если путь неприемлем.

Правила, которые задача обязана реализовать ровно в этом порядке:

1. `\` → `/`.
2. Разбить по `/`, выбросить пустые компоненты и `.`.
3. Любой компонент `..` → `nil` (обход вверх недопустим).
4. Санитизация каждого компонента: символы `<>:"|?*`, управляющие символы и `\0` → `_`; обрезать хвостовые точки и пробелы (Windows не умеет такие имена).
5. Зарезервированные Windows-имена (`CON`, `PRN`, `AUX`, `NUL`, `COM1`–`COM9`, `LPT1`–`LPT9`, регистронезависимо, по части до первой точки) → к компоненту добавляется `_`.
6. Компонент длиннее 150 символов или итоговый путь длиннее 400 символов → `nil`.
7. Компонентов не осталось → `nil`.

Абсолютные пути и `C:`-префиксы не отбрасываются, а обезвреживаются шагами 2 и 4: `/etc/passwd` → `etc/passwd`, `C:\Users\x` → `C_/Users/x`. Результат всегда остаётся внутри партии.

- [ ] **Step 1: Написать падающие тесты**

`mac/Tests/LanClipCoreTests/RelPathTests.swift`:

```swift
import XCTest
@testable import LanClipCore

final class RelPathTests: XCTestCase {
    func testKeepsSimplePath() {
        XCTAssertEqual(RelPath.normalize("img/a.png"), "img/a.png")
    }

    func testConvertsBackslashesAndCollapsesSeparators() {
        XCTAssertEqual(RelPath.normalize("img\\\\sub//a.png"), "img/sub/a.png")
    }

    func testRejectsTraversal() {
        XCTAssertNil(RelPath.normalize("../secret"))
        XCTAssertNil(RelPath.normalize("img/../../secret"))
    }

    func testStripsAbsolutePrefix() {
        XCTAssertEqual(RelPath.normalize("/etc/passwd"), "etc/passwd")
    }

    func testSanitizesDriveLetterAndIllegalChars() {
        XCTAssertEqual(RelPath.normalize("C:\\\\Users\\\\a?b.txt"), "C_/Users/a_b.txt")
    }

    func testKeepsCyrillicAndSpaces() {
        XCTAssertEqual(RelPath.normalize("папка/мой отчёт.pdf"), "папка/мой отчёт.pdf")
    }

    func testTrimsTrailingDotsAndSpaces() {
        XCTAssertEqual(RelPath.normalize("name. ."), "name")
    }

    func testSuffixesReservedWindowsNames() {
        XCTAssertEqual(RelPath.normalize("NUL"), "NUL_")
        XCTAssertEqual(RelPath.normalize("com1.txt"), "com1.txt_")
    }

    func testRejectsOverlongComponentAndPath() {
        XCTAssertNil(RelPath.normalize(String(repeating: "a", count: 151)))
        let deep = (0..<10).map { _ in String(repeating: "b", count: 45) }.joined(separator: "/")
        XCTAssertNil(RelPath.normalize(deep))
    }

    func testRejectsEmptyResult() {
        XCTAssertNil(RelPath.normalize(""))
        XCTAssertNil(RelPath.normalize("./././"))
    }
}
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `cd mac && swift test --filter RelPathTests`
Expected: FAIL — `cannot find 'RelPath' in scope`.

- [ ] **Step 3: Реализовать нормализацию**

`mac/Sources/LanClipCore/RelPath.swift`:

```swift
import Foundation

public enum RelPath {
    static let maxComponent = 150
    static let maxTotal = 400

    private static let illegal: Set<Character> = ["<", ">", ":", "\"", "|", "?", "*", "\0"]

    private static let reserved: Set<String> = {
        var names: Set<String> = ["con", "prn", "aux", "nul"]
        for n in 1...9 {
            names.insert("com\(n)")
            names.insert("lpt\(n)")
        }
        return names
    }()

    public static func normalize(_ raw: String) -> String? {
        let unified = raw.replacingOccurrences(of: "\\", with: "/")
        var components: [String] = []

        for piece in unified.split(separator: "/", omittingEmptySubsequences: true) {
            let component = String(piece)
            if component == "." { continue }
            if component == ".." { return nil }
            guard let safe = sanitize(component) else { return nil }
            components.append(safe)
        }

        guard !components.isEmpty else { return nil }
        let joined = components.joined(separator: "/")
        guard joined.count <= maxTotal else { return nil }
        return joined
    }

    private static func sanitize(_ component: String) -> String? {
        var cleaned = String(component.map { char in
            if illegal.contains(char) { return "_" }
            if let scalar = char.unicodeScalars.first,
               char.unicodeScalars.count == 1,
               scalar.properties.generalCategory == .control {
                return "_"
            }
            return char
        })

        while let last = cleaned.last, last == "." || last == " " {
            cleaned.removeLast()
        }

        guard !cleaned.isEmpty else { return nil }
        guard cleaned.count <= maxComponent else { return nil }

        let base = cleaned.split(separator: ".", maxSplits: 1).first.map(String.init) ?? cleaned
        if reserved.contains(base.lowercased()) {
            cleaned += "_"
        }
        return cleaned
    }
}
```

- [ ] **Step 4: Проверить, что тесты проходят**

Run: `cd mac && swift test --filter RelPathTests`
Expected: PASS, 10 тестов.

- [ ] **Step 5: Коммит**

```bash
git add mac/Sources/LanClipCore/RelPath.swift mac/Tests/LanClipCoreTests/RelPathTests.swift
git commit -m "feat(mac): нормализация относительных путей с защитой от обхода каталогов"
```

---

### Task 3: Mac — манифест и его JSON

**Files:**
- Create: `mac/Sources/LanClipCore/Manifest.swift`
- Test: `mac/Tests/LanClipCoreTests/ManifestTests.swift`

**Interfaces:**
- Consumes: ничего.
- Produces:

```swift
public enum ClipKind: String, Codable, Sendable { case empty, text, image, files }

public struct BlobRef: Codable, Equatable, Sendable {
    public let i: Int
    public let rel: String
    public let size: Int
    public let mime: String?
    public init(i: Int, rel: String, size: Int, mime: String? = nil)
}

public struct Manifest: Codable, Equatable, Sendable {
    public let kind: ClipKind
    public let seq: Int
    public let text: String?
    public let blobs: [BlobRef]?
    public let totalSize: Int?

    public static func empty(seq: Int) -> Manifest
    public static func text(_ value: String, seq: Int) -> Manifest
    public static func image(pngSize: Int, seq: Int) -> Manifest
    public static func files(_ blobs: [BlobRef], seq: Int) -> Manifest

    public func encoded() throws -> Data
    public static func decode(_ data: Data) throws -> Manifest
}
```

`totalSize` считается конструкторами как сумма `size` блобов и присутствует только у `image` и `files`. Для картинки `rel` всегда `clip.png`, `mime` всегда `image/png`.

- [ ] **Step 1: Написать падающие тесты**

`mac/Tests/LanClipCoreTests/ManifestTests.swift`:

```swift
import XCTest
@testable import LanClipCore

final class ManifestTests: XCTestCase {
    private func json(_ manifest: Manifest) throws -> [String: Any] {
        let data = try manifest.encoded()
        return try XCTUnwrap(JSONSerialization.jsonObject(with: data) as? [String: Any])
    }

    func testEmptyOmitsOptionalKeys() throws {
        let object = try json(.empty(seq: 41))
        XCTAssertEqual(object["kind"] as? String, "empty")
        XCTAssertEqual(object["seq"] as? Int, 41)
        XCTAssertNil(object["text"])
        XCTAssertNil(object["blobs"])
        XCTAssertNil(object["totalSize"])
    }

    func testTextCarriesTextOnly() throws {
        let object = try json(.text("привет 👋", seq: 42))
        XCTAssertEqual(object["kind"] as? String, "text")
        XCTAssertEqual(object["text"] as? String, "привет 👋")
        XCTAssertNil(object["blobs"])
        XCTAssertNil(object["totalSize"])
    }

    func testImageDescribesSinglePngBlob() throws {
        let manifest = Manifest.image(pngSize: 48213, seq: 43)
        XCTAssertEqual(manifest.totalSize, 48213)
        let blob = try XCTUnwrap(manifest.blobs?.first)
        XCTAssertEqual(blob.i, 0)
        XCTAssertEqual(blob.rel, "clip.png")
        XCTAssertEqual(blob.size, 48213)
        XCTAssertEqual(blob.mime, "image/png")
    }

    func testFilesSumsTotalSize() {
        let manifest = Manifest.files([
            BlobRef(i: 0, rel: "отчёт.pdf", size: 91234),
            BlobRef(i: 1, rel: "img/a.png", size: 5120),
        ], seq: 44)
        XCTAssertEqual(manifest.totalSize, 96354)
        XCTAssertEqual(manifest.kind, .files)
    }

    func testRoundTripsThroughJson() throws {
        let original = Manifest.files([BlobRef(i: 0, rel: "папка/файл.txt", size: 7)], seq: 9)
        XCTAssertEqual(try Manifest.decode(try original.encoded()), original)
    }

    func testDecodesManifestFromForeignAgent() throws {
        let raw = Data(#"{"kind":"text","seq":5,"text":"hi"}"#.utf8)
        let manifest = try Manifest.decode(raw)
        XCTAssertEqual(manifest.kind, .text)
        XCTAssertEqual(manifest.text, "hi")
        XCTAssertNil(manifest.blobs)
    }

    func testRejectsUnknownKind() {
        let raw = Data(#"{"kind":"video","seq":1}"#.utf8)
        XCTAssertThrowsError(try Manifest.decode(raw))
    }
}
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `cd mac && swift test --filter ManifestTests`
Expected: FAIL — `cannot find 'Manifest' in scope`.

- [ ] **Step 3: Реализовать манифест**

`mac/Sources/LanClipCore/Manifest.swift`:

```swift
import Foundation

public enum ClipKind: String, Codable, Sendable {
    case empty, text, image, files
}

public struct BlobRef: Codable, Equatable, Sendable {
    public let i: Int
    public let rel: String
    public let size: Int
    public let mime: String?

    public init(i: Int, rel: String, size: Int, mime: String? = nil) {
        self.i = i
        self.rel = rel
        self.size = size
        self.mime = mime
    }
}

public struct Manifest: Codable, Equatable, Sendable {
    public let kind: ClipKind
    public let seq: Int
    public let text: String?
    public let blobs: [BlobRef]?
    public let totalSize: Int?

    private init(kind: ClipKind, seq: Int, text: String?, blobs: [BlobRef]?) {
        self.kind = kind
        self.seq = seq
        self.text = text
        self.blobs = blobs
        self.totalSize = blobs.map { list in list.reduce(0) { $0 + $1.size } }
    }

    public static func empty(seq: Int) -> Manifest {
        Manifest(kind: .empty, seq: seq, text: nil, blobs: nil)
    }

    public static func text(_ value: String, seq: Int) -> Manifest {
        Manifest(kind: .text, seq: seq, text: value, blobs: nil)
    }

    public static func image(pngSize: Int, seq: Int) -> Manifest {
        Manifest(kind: .image, seq: seq, text: nil,
                 blobs: [BlobRef(i: 0, rel: "clip.png", size: pngSize, mime: "image/png")])
    }

    public static func files(_ blobs: [BlobRef], seq: Int) -> Manifest {
        Manifest(kind: .files, seq: seq, text: nil, blobs: blobs)
    }

    public func encoded() throws -> Data {
        let encoder = JSONEncoder()
        encoder.outputFormatting = [.withoutEscapingSlashes]
        return try encoder.encode(self)
    }

    public static func decode(_ data: Data) throws -> Manifest {
        try JSONDecoder().decode(Manifest.self, from: data)
    }
}
```

Синтезированный `Codable` в Swift кодирует опциональные поля через `encodeIfPresent`, поэтому `null`-ключи в JSON не появляются — именно это и требует протокол.

- [ ] **Step 4: Проверить, что тесты проходят**

Run: `cd mac && swift test --filter ManifestTests`
Expected: PASS, 7 тестов.

- [ ] **Step 5: Коммит**

```bash
git add mac/Sources/LanClipCore/Manifest.swift mac/Tests/LanClipCoreTests/ManifestTests.swift
git commit -m "feat(mac): модель манифеста буфера и её JSON-представление"
```

---

### Task 4: Mac — конфиг и генерация токена

**Files:**
- Create: `mac/Sources/LanClipCore/Config.swift`
- Test: `mac/Tests/LanClipCoreTests/ConfigTests.swift`

**Interfaces:**
- Consumes: ничего.
- Produces:

```swift
public struct Config: Codable, Equatable, Sendable {
    public var port: Int
    public var token: String
    public var peers: [String]
    public var maxBytes: Int
    public var autoPaste: Bool

    public static let defaultPort = 8899
    public static let defaultMaxBytes = 536_870_912
    public static var defaultURL: URL { get }          // ~/.config/lanclip/config.json

    public init(port: Int = Config.defaultPort, token: String, peers: [String],
                maxBytes: Int = Config.defaultMaxBytes, autoPaste: Bool = true)

    public static func generateToken() -> String        // 32 шестнадцатеричных символа
    public static func load(at url: URL) throws -> Config   // создаёт файл, если его нет
    public func validate() throws
}

public enum ConfigError: Error, Equatable {
    case invalidPort(Int)
    case emptyToken
    case noPeers
    case invalidMaxBytes(Int)
    case malformed(String)
}
```

`load` при отсутствии файла создаёт его с новым токеном, пустым `peers` и правами `0600`, после чего возвращает конфиг — а `validate()` уже сообщает про `noPeers`. Отсутствующие ключи в существующем файле берут значения по умолчанию; `token` в существующем файле обязателен.

- [ ] **Step 1: Написать падающие тесты**

`mac/Tests/LanClipCoreTests/ConfigTests.swift`:

```swift
import XCTest
@testable import LanClipCore

final class ConfigTests: XCTestCase {
    private var directory: URL!

    override func setUpWithError() throws {
        directory = URL(fileURLWithPath: NSTemporaryDirectory())
            .appendingPathComponent("lanclip-config-\(UUID().uuidString)")
        try FileManager.default.createDirectory(at: directory, withIntermediateDirectories: true)
    }

    override func tearDownWithError() throws {
        try? FileManager.default.removeItem(at: directory)
    }

    private func write(_ json: String) throws -> URL {
        let url = directory.appendingPathComponent("config.json")
        try Data(json.utf8).write(to: url)
        return url
    }

    func testGeneratesThirtyTwoHexToken() {
        let token = Config.generateToken()
        XCTAssertEqual(token.count, 32)
        XCTAssertTrue(token.allSatisfy { $0.isHexDigit && !$0.isUppercase })
        XCTAssertNotEqual(token, Config.generateToken())
    }

    func testCreatesFileWhenMissing() throws {
        let url = directory.appendingPathComponent("config.json")
        let config = try Config.load(at: url)

        XCTAssertTrue(FileManager.default.fileExists(atPath: url.path))
        XCTAssertEqual(config.port, 8899)
        XCTAssertEqual(config.maxBytes, 536_870_912)
        XCTAssertTrue(config.autoPaste)
        XCTAssertEqual(config.token.count, 32)
        XCTAssertTrue(config.peers.isEmpty)

        let permissions = try FileManager.default.attributesOfItem(atPath: url.path)[.posixPermissions] as? NSNumber
        XCTAssertEqual(permissions?.int16Value, 0o600)
    }

    func testAppliesDefaultsForMissingKeys() throws {
        let url = try write(#"{"token":"abc","peers":["pc"]}"#)
        let config = try Config.load(at: url)
        XCTAssertEqual(config.port, 8899)
        XCTAssertEqual(config.maxBytes, 536_870_912)
        XCTAssertTrue(config.autoPaste)
        XCTAssertEqual(config.peers, ["pc"])
    }

    func testReadsExplicitValues() throws {
        let url = try write(#"{"port":9001,"token":"t","peers":["a","b"],"maxBytes":1024,"autoPaste":false}"#)
        let config = try Config.load(at: url)
        XCTAssertEqual(config, Config(port: 9001, token: "t", peers: ["a", "b"],
                                      maxBytes: 1024, autoPaste: false))
    }

    func testRejectsMalformedJson() throws {
        let url = try write("{ не json")
        XCTAssertThrowsError(try Config.load(at: url)) { error in
            guard case ConfigError.malformed = error else {
                return XCTFail("ожидалась malformed, получено \(error)")
            }
        }
    }

    func testValidateRejectsBadValues() {
        XCTAssertThrowsError(try Config(port: 0, token: "t", peers: ["pc"]).validate()) { error in
            XCTAssertEqual(error as? ConfigError, .invalidPort(0))
        }
        XCTAssertThrowsError(try Config(token: "", peers: ["pc"]).validate()) { error in
            XCTAssertEqual(error as? ConfigError, .emptyToken)
        }
        XCTAssertThrowsError(try Config(token: "t", peers: []).validate()) { error in
            XCTAssertEqual(error as? ConfigError, .noPeers)
        }
        XCTAssertThrowsError(try Config(token: "t", peers: ["pc"], maxBytes: 0).validate()) { error in
            XCTAssertEqual(error as? ConfigError, .invalidMaxBytes(0))
        }
    }

    func testValidateAcceptsGoodConfig() throws {
        try Config(token: "t", peers: ["pc"]).validate()
    }
}
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `cd mac && swift test --filter ConfigTests`
Expected: FAIL — `cannot find 'Config' in scope`.

- [ ] **Step 3: Реализовать конфиг**

`mac/Sources/LanClipCore/Config.swift`:

```swift
import Foundation

public enum ConfigError: Error, Equatable {
    case invalidPort(Int)
    case emptyToken
    case noPeers
    case invalidMaxBytes(Int)
    case malformed(String)
}

public struct Config: Codable, Equatable, Sendable {
    public var port: Int
    public var token: String
    public var peers: [String]
    public var maxBytes: Int
    public var autoPaste: Bool

    public static let defaultPort = 8899
    public static let defaultMaxBytes = 536_870_912

    public static var defaultURL: URL {
        FileManager.default.homeDirectoryForCurrentUser
            .appendingPathComponent(".config/lanclip/config.json")
    }

    public init(port: Int = Config.defaultPort, token: String, peers: [String],
                maxBytes: Int = Config.defaultMaxBytes, autoPaste: Bool = true) {
        self.port = port
        self.token = token
        self.peers = peers
        self.maxBytes = maxBytes
        self.autoPaste = autoPaste
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        port = try container.decodeIfPresent(Int.self, forKey: .port) ?? Config.defaultPort
        token = try container.decode(String.self, forKey: .token)
        peers = try container.decodeIfPresent([String].self, forKey: .peers) ?? []
        maxBytes = try container.decodeIfPresent(Int.self, forKey: .maxBytes) ?? Config.defaultMaxBytes
        autoPaste = try container.decodeIfPresent(Bool.self, forKey: .autoPaste) ?? true
    }

    public static func generateToken() -> String {
        var bytes = [UInt8](repeating: 0, count: 16)
        for index in bytes.indices { bytes[index] = UInt8.random(in: 0...255) }
        return bytes.map { String(format: "%02x", $0) }.joined()
    }

    public static func load(at url: URL) throws -> Config {
        if !FileManager.default.fileExists(atPath: url.path) {
            let fresh = Config(token: generateToken(), peers: [])
            try fresh.write(to: url)
            return fresh
        }

        let data = try Data(contentsOf: url)
        do {
            return try JSONDecoder().decode(Config.self, from: data)
        } catch {
            throw ConfigError.malformed(String(describing: error))
        }
    }

    public func write(to url: URL) throws {
        try FileManager.default.createDirectory(at: url.deletingLastPathComponent(),
                                               withIntermediateDirectories: true)
        let encoder = JSONEncoder()
        encoder.outputFormatting = [.prettyPrinted, .withoutEscapingSlashes]
        try encoder.encode(self).write(to: url)
        try FileManager.default.setAttributes([.posixPermissions: 0o600], ofItemAtPath: url.path)
    }

    public func validate() throws {
        guard port > 0, port < 65_536 else { throw ConfigError.invalidPort(port) }
        guard !token.isEmpty else { throw ConfigError.emptyToken }
        guard !peers.isEmpty else { throw ConfigError.noPeers }
        guard maxBytes > 0 else { throw ConfigError.invalidMaxBytes(maxBytes) }
    }
}
```

- [ ] **Step 4: Проверить, что тесты проходят**

Run: `cd mac && swift test --filter ConfigTests`
Expected: PASS, 7 тестов.

- [ ] **Step 5: Коммит**

```bash
git add mac/Sources/LanClipCore/Config.swift mac/Tests/LanClipCoreTests/ConfigTests.swift
git commit -m "feat(mac): конфиг lanclip с генерацией токена и валидацией"
```

---

### Task 5: Mac — абстракция буфера, снимок и обход папок

Здесь появляется граница, отделяющая ядро от платформы, и вместе с ней подставной буфер, на котором держатся все дальнейшие тесты протокола.

**Files:**
- Create: `mac/Sources/LanClipCore/Clipboard.swift`
- Create: `mac/Sources/LanClipCore/Snapshot.swift`
- Test: `mac/Tests/LanClipCoreTests/SnapshotTests.swift`
- Test: `mac/Tests/LanClipCoreTests/FakeClipboard.swift`

**Interfaces:**
- Consumes: `Manifest`, `BlobRef`, `RelPath`.
- Produces:

```swift
public enum ClipContent: Equatable, Sendable {
    case empty
    case text(String)
    case image(Data)          // PNG
    case files([URL])         // абсолютные локальные пути
}

public protocol ClipboardReading: AnyObject {
    func changeCount() -> Int
    func read() throws -> ClipContent
}

public protocol ClipboardWriting: AnyObject {
    func write(_ content: ClipContent) throws
}

public struct ClipSnapshot: Sendable {
    public let manifest: Manifest
    public let text: String?
    public let imagePNG: Data?
    public let sources: [Int: URL]      // индекс блоба → абсолютный путь, только для .files
}

public final class SnapshotStore {
    public init(reader: ClipboardReading, fileManager: FileManager = .default)
    public func current() throws -> ClipSnapshot        // пересчитывает при смене changeCount
    public func blob(index: Int, seq: Int) throws -> Data?   // nil — индекс вне диапазона
}

public enum SnapshotError: Error, Equatable { case staleSeq }
```

`blob` бросает `SnapshotError.staleSeq`, если запрошенный `seq` не совпадает с текущим снимком, и возвращает `nil`, если индекс вне диапазона: сервер превращает первое в `409`, второе в `404`.

- [ ] **Step 1: Написать подставной буфер**

`mac/Tests/LanClipCoreTests/FakeClipboard.swift` — используется этой и всеми последующими задачами:

```swift
import Foundation
@testable import LanClipCore

final class FakeClipboard: ClipboardReading, ClipboardWriting, @unchecked Sendable {
    private(set) var changes = 1
    private(set) var written: [ClipContent] = []
    var content: ClipContent = .empty {
        didSet { changes += 1 }
    }

    func changeCount() -> Int { changes }
    func read() throws -> ClipContent { content }

    func write(_ content: ClipContent) throws {
        written.append(content)
        self.content = content
    }
}
```

- [ ] **Step 2: Написать падающие тесты снимка**

`mac/Tests/LanClipCoreTests/SnapshotTests.swift`:

```swift
import XCTest
@testable import LanClipCore

final class SnapshotTests: XCTestCase {
    private var directory: URL!
    private let clipboard = FakeClipboard()

    override func setUpWithError() throws {
        directory = URL(fileURLWithPath: NSTemporaryDirectory())
            .appendingPathComponent("lanclip-snapshot-\(UUID().uuidString)")
        try FileManager.default.createDirectory(at: directory, withIntermediateDirectories: true)
    }

    override func tearDownWithError() throws {
        try? FileManager.default.removeItem(at: directory)
    }

    private func makeFile(_ relative: String, _ body: String) throws -> URL {
        let url = directory.appendingPathComponent(relative)
        try FileManager.default.createDirectory(at: url.deletingLastPathComponent(),
                                               withIntermediateDirectories: true)
        try Data(body.utf8).write(to: url)
        return url
    }

    func testEmptyClipboardYieldsEmptyManifest() throws {
        let store = SnapshotStore(reader: clipboard)
        XCTAssertEqual(try store.current().manifest.kind, .empty)
    }

    func testTextSnapshotCarriesSeqFromChangeCount() throws {
        clipboard.content = .text("привет")
        let store = SnapshotStore(reader: clipboard)
        let snapshot = try store.current()
        XCTAssertEqual(snapshot.manifest.kind, .text)
        XCTAssertEqual(snapshot.manifest.text, "привет")
        XCTAssertEqual(snapshot.manifest.seq, clipboard.changeCount())
    }

    func testImageBlobReturnsPngBytes() throws {
        let png = Data([0x89, 0x50, 0x4E, 0x47, 1, 2, 3])
        clipboard.content = .image(png)
        let store = SnapshotStore(reader: clipboard)
        let snapshot = try store.current()
        XCTAssertEqual(snapshot.manifest.totalSize, png.count)
        XCTAssertEqual(try store.blob(index: 0, seq: snapshot.manifest.seq), png)
    }

    func testSingleFileUsesBareName() throws {
        let file = try makeFile("отчёт.pdf", "данные")
        clipboard.content = .files([file])
        let store = SnapshotStore(reader: clipboard)
        let snapshot = try store.current()
        XCTAssertEqual(snapshot.manifest.blobs?.map { $0.rel }, ["отчёт.pdf"])
        XCTAssertEqual(try store.blob(index: 0, seq: snapshot.manifest.seq), Data("данные".utf8))
    }

    func testFolderIsWalkedRecursivelyWithRelativePaths() throws {
        _ = try makeFile("папка/a.txt", "a")
        _ = try makeFile("папка/вложенная/b.txt", "bb")
        clipboard.content = .files([directory.appendingPathComponent("папка")])
        let store = SnapshotStore(reader: clipboard)
        let snapshot = try store.current()
        XCTAssertEqual(snapshot.manifest.blobs?.map { $0.rel }.sorted(),
                       ["папка/a.txt", "папка/вложенная/b.txt"])
        XCTAssertEqual(snapshot.manifest.totalSize, 3)
    }

    func testStaleSeqIsRejected() throws {
        clipboard.content = .text("первый")
        let store = SnapshotStore(reader: clipboard)
        let stale = try store.current().manifest.seq
        clipboard.content = .image(Data([1, 2, 3]))
        _ = try store.current()
        XCTAssertThrowsError(try store.blob(index: 0, seq: stale)) { error in
            XCTAssertEqual(error as? SnapshotError, .staleSeq)
        }
    }

    func testOutOfRangeIndexReturnsNil() throws {
        clipboard.content = .image(Data([1]))
        let store = SnapshotStore(reader: clipboard)
        let seq = try store.current().manifest.seq
        XCTAssertNil(try store.blob(index: 5, seq: seq))
    }
}
```

- [ ] **Step 3: Убедиться, что тесты падают**

Run: `cd mac && swift test --filter SnapshotTests`
Expected: FAIL — `cannot find 'SnapshotStore' in scope`.

- [ ] **Step 4: Реализовать абстракцию буфера**

`mac/Sources/LanClipCore/Clipboard.swift`:

```swift
import Foundation

public enum ClipContent: Equatable, Sendable {
    case empty
    case text(String)
    case image(Data)
    case files([URL])
}

public protocol ClipboardReading: AnyObject {
    func changeCount() -> Int
    func read() throws -> ClipContent
}

public protocol ClipboardWriting: AnyObject {
    func write(_ content: ClipContent) throws
}
```

- [ ] **Step 5: Реализовать снимок и обход папок**

`mac/Sources/LanClipCore/Snapshot.swift`:

```swift
import Foundation

public enum SnapshotError: Error, Equatable {
    case staleSeq
}

public struct ClipSnapshot: Sendable {
    public let manifest: Manifest
    public let text: String?
    public let imagePNG: Data?
    public let sources: [Int: URL]
}

public final class SnapshotStore {
    private let reader: ClipboardReading
    private let fileManager: FileManager
    private var cached: ClipSnapshot?

    public init(reader: ClipboardReading, fileManager: FileManager = .default) {
        self.reader = reader
        self.fileManager = fileManager
    }

    public func current() throws -> ClipSnapshot {
        let seq = reader.changeCount()
        if let cached, cached.manifest.seq == seq { return cached }

        let snapshot = try build(seq: seq, content: try reader.read())
        cached = snapshot
        return snapshot
    }

    public func blob(index: Int, seq: Int) throws -> Data? {
        let snapshot = try current()
        guard snapshot.manifest.seq == seq else { throw SnapshotError.staleSeq }

        if let png = snapshot.imagePNG {
            return index == 0 ? png : nil
        }
        guard let source = snapshot.sources[index] else { return nil }
        return try Data(contentsOf: source)
    }

    private func build(seq: Int, content: ClipContent) throws -> ClipSnapshot {
        switch content {
        case .empty:
            return ClipSnapshot(manifest: .empty(seq: seq), text: nil, imagePNG: nil, sources: [:])

        case .text(let value):
            return ClipSnapshot(manifest: .text(value, seq: seq), text: value,
                                imagePNG: nil, sources: [:])

        case .image(let png):
            return ClipSnapshot(manifest: .image(pngSize: png.count, seq: seq), text: nil,
                                imagePNG: png, sources: [:])

        case .files(let urls):
            var blobs: [BlobRef] = []
            var sources: [Int: URL] = [:]

            for url in urls {
                for entry in try expand(url) {
                    guard let rel = RelPath.normalize(entry.rel) else { continue }
                    sources[blobs.count] = entry.url
                    blobs.append(BlobRef(i: blobs.count, rel: rel, size: entry.size))
                }
            }

            if blobs.isEmpty {
                return ClipSnapshot(manifest: .empty(seq: seq), text: nil, imagePNG: nil, sources: [:])
            }
            return ClipSnapshot(manifest: .files(blobs, seq: seq), text: nil,
                                imagePNG: nil, sources: sources)
        }
    }

    private struct Entry {
        let url: URL
        let rel: String
        let size: Int
    }

    private func expand(_ url: URL) throws -> [Entry] {
        var isDirectory: ObjCBool = false
        guard fileManager.fileExists(atPath: url.path, isDirectory: &isDirectory) else { return [] }

        if !isDirectory.boolValue {
            return [Entry(url: url, rel: url.lastPathComponent, size: try size(of: url))]
        }

        let base = url.lastPathComponent
        guard let walker = fileManager.enumerator(at: url, includingPropertiesForKeys: [.isDirectoryKey]) else {
            return []
        }

        var entries: [Entry] = []
        for case let child as URL in walker {
            let values = try child.resourceValues(forKeys: [.isDirectoryKey])
            if values.isDirectory == true { continue }
            let relative = child.path.dropFirst(url.path.count).drop(while: { $0 == "/" })
            entries.append(Entry(url: child, rel: base + "/" + relative, size: try size(of: child)))
        }
        return entries
    }

    private func size(of url: URL) throws -> Int {
        let attributes = try fileManager.attributesOfItem(atPath: url.path)
        return (attributes[.size] as? NSNumber)?.intValue ?? 0
    }
}
```

- [ ] **Step 6: Проверить, что тесты проходят**

Run: `cd mac && swift test --filter SnapshotTests`
Expected: PASS, 7 тестов.

- [ ] **Step 7: Коммит**

```bash
git add mac/Sources/LanClipCore/Clipboard.swift mac/Sources/LanClipCore/Snapshot.swift \
        mac/Tests/LanClipCoreTests/SnapshotTests.swift mac/Tests/LanClipCoreTests/FakeClipboard.swift
git commit -m "feat(mac): абстракция буфера, снимок манифеста и рекурсивный обход папок"
```

---

### Task 6: Mac — HTTP-сообщения и проверка приватности адреса

**Files:**
- Create: `mac/Sources/LanClipCore/HttpMessage.swift`
- Create: `mac/Sources/LanClipCore/Net.swift`
- Test: `mac/Tests/LanClipCoreTests/HttpMessageTests.swift`
- Test: `mac/Tests/LanClipCoreTests/NetTests.swift`

**Interfaces:**
- Consumes: ничего.
- Produces:

```swift
public struct HttpRequest: Equatable, Sendable {
    public let method: String
    public let path: String
    public let query: [String: String]
    public let headers: [String: String]     // ключи приведены к нижнему регистру
    public let body: Data
}

public struct HttpResponse: Sendable {
    public let status: Int
    public let headers: [String: String]
    public let body: Data?
    public init(status: Int, headers: [String: String] = [:], body: Data? = nil)
    public static func json(_ status: Int, _ payload: Data) -> HttpResponse
    public static func bytes(_ payload: Data) -> HttpResponse
    public static func empty(_ status: Int) -> HttpResponse
    public func head() -> Data               // строка статуса и заголовки, включая Content-Length
}

public enum HttpParseError: Error, Equatable { case incomplete, malformed }

public func parseHttpRequest(_ data: Data) throws -> HttpRequest
public func parseHttpResponse(_ data: Data) throws -> (status: Int, headers: [String: String], body: Data)
public func isPrivateAddress(_ address: String) -> Bool
```

`parseHttpRequest` бросает `.incomplete`, если заголовки ещё не закончились или тело короче `Content-Length` — вызывающий продолжает накапливать байты.

- [ ] **Step 1: Написать падающие тесты HTTP**

`mac/Tests/LanClipCoreTests/HttpMessageTests.swift`:

```swift
import XCTest
@testable import LanClipCore

final class HttpMessageTests: XCTestCase {
    func testParsesRequestLineHeadersAndQuery() throws {
        let raw = Data("GET /clip/blob/2?seq=44 HTTP/1.1\r\nHost: pc:8899\r\nX-Clip-Token: abc\r\n\r\n".utf8)
        let request = try parseHttpRequest(raw)
        XCTAssertEqual(request.method, "GET")
        XCTAssertEqual(request.path, "/clip/blob/2")
        XCTAssertEqual(request.query["seq"], "44")
        XCTAssertEqual(request.headers["x-clip-token"], "abc")
        XCTAssertTrue(request.body.isEmpty)
    }

    func testThrowsIncompleteUntilHeadersEnd() {
        XCTAssertThrowsError(try parseHttpRequest(Data("GET / HTTP/1.1\r\nHost: x".utf8))) { error in
            XCTAssertEqual(error as? HttpParseError, .incomplete)
        }
    }

    func testThrowsIncompleteUntilBodyArrives() {
        let raw = Data("POST /pull HTTP/1.1\r\nContent-Length: 5\r\n\r\nab".utf8)
        XCTAssertThrowsError(try parseHttpRequest(raw)) { error in
            XCTAssertEqual(error as? HttpParseError, .incomplete)
        }
    }

    func testReadsBodyOfDeclaredLength() throws {
        let raw = Data("POST /pull HTTP/1.1\r\nContent-Length: 5\r\n\r\nabcde".utf8)
        XCTAssertEqual(try parseHttpRequest(raw).body, Data("abcde".utf8))
    }

    func testRejectsGarbageRequestLine() {
        XCTAssertThrowsError(try parseHttpRequest(Data("ГАРБИДЖ\r\n\r\n".utf8))) { error in
            XCTAssertEqual(error as? HttpParseError, .malformed)
        }
    }

    func testResponseHeadCarriesContentLength() throws {
        let head = String(decoding: HttpResponse.json(200, Data("{}".utf8)).head(), as: UTF8.self)
        XCTAssertTrue(head.hasPrefix("HTTP/1.1 200 OK\r\n"))
        XCTAssertTrue(head.contains("Content-Length: 2\r\n"))
        XCTAssertTrue(head.contains("Content-Type: application/json; charset=utf-8\r\n"))
        XCTAssertTrue(head.hasSuffix("\r\n\r\n"))
    }

    func testEmptyResponseDeclaresZeroLength() {
        let head = String(decoding: HttpResponse.empty(401).head(), as: UTF8.self)
        XCTAssertTrue(head.hasPrefix("HTTP/1.1 401 Unauthorized\r\n"))
        XCTAssertTrue(head.contains("Content-Length: 0\r\n"))
    }

    func testParsesResponse() throws {
        let raw = Data("HTTP/1.1 200 OK\r\nContent-Length: 3\r\n\r\nabc".utf8)
        let parsed = try parseHttpResponse(raw)
        XCTAssertEqual(parsed.status, 200)
        XCTAssertEqual(parsed.headers["content-length"], "3")
        XCTAssertEqual(parsed.body, Data("abc".utf8))
    }
}
```

- [ ] **Step 2: Написать падающие тесты приватности адреса**

`mac/Tests/LanClipCoreTests/NetTests.swift`:

```swift
import XCTest
@testable import LanClipCore

final class NetTests: XCTestCase {
    func testAcceptsPrivateRanges() {
        for address in ["192.168.1.184", "10.0.0.5", "172.16.3.9", "172.31.255.255",
                        "127.0.0.1", "::1", "fe80::1%en0"] {
            XCTAssertTrue(isPrivateAddress(address), address)
        }
    }

    func testRejectsPublicAddresses() {
        for address in ["83.222.27.227", "8.8.8.8", "172.32.0.1", "2606:4700::1111", ""] {
            XCTAssertFalse(isPrivateAddress(address), address)
        }
    }
}
```

- [ ] **Step 3: Убедиться, что тесты падают**

Run: `cd mac && swift test --filter HttpMessageTests && swift test --filter NetTests`
Expected: FAIL — `cannot find 'parseHttpRequest' in scope`.

- [ ] **Step 4: Реализовать HTTP-сообщения**

`mac/Sources/LanClipCore/HttpMessage.swift`:

```swift
import Foundation

public enum HttpParseError: Error, Equatable {
    case incomplete
    case malformed
}

public struct HttpRequest: Equatable, Sendable {
    public let method: String
    public let path: String
    public let query: [String: String]
    public let headers: [String: String]
    public let body: Data
}

public struct HttpResponse: Sendable {
    public let status: Int
    public let headers: [String: String]
    public let body: Data?

    public init(status: Int, headers: [String: String] = [:], body: Data? = nil) {
        self.status = status
        self.headers = headers
        self.body = body
    }

    public static func json(_ status: Int, _ payload: Data) -> HttpResponse {
        HttpResponse(status: status,
                     headers: ["Content-Type": "application/json; charset=utf-8"],
                     body: payload)
    }

    public static func bytes(_ payload: Data) -> HttpResponse {
        HttpResponse(status: 200,
                     headers: ["Content-Type": "application/octet-stream"],
                     body: payload)
    }

    public static func empty(_ status: Int) -> HttpResponse {
        HttpResponse(status: status)
    }

    public func head() -> Data {
        var text = "HTTP/1.1 \(status) \(HttpResponse.reason(status))\r\n"
        for (key, value) in headers.sorted(by: { $0.key < $1.key }) {
            text += "\(key): \(value)\r\n"
        }
        text += "Content-Length: \(body?.count ?? 0)\r\n"
        text += "Connection: close\r\n\r\n"
        return Data(text.utf8)
    }

    static func reason(_ status: Int) -> String {
        switch status {
        case 200: return "OK"
        case 400: return "Bad Request"
        case 401: return "Unauthorized"
        case 403: return "Forbidden"
        case 404: return "Not Found"
        case 405: return "Method Not Allowed"
        case 409: return "Conflict"
        case 500: return "Internal Server Error"
        case 503: return "Service Unavailable"
        default: return "Status"
        }
    }
}

private let crlfcrlf = Data("\r\n\r\n".utf8)

public func parseHttpRequest(_ data: Data) throws -> HttpRequest {
    guard let separator = data.range(of: crlfcrlf) else { throw HttpParseError.incomplete }

    let headText = String(decoding: data[data.startIndex..<separator.lowerBound], as: UTF8.self)
    var lines = headText.components(separatedBy: "\r\n")
    guard let requestLine = lines.first else { throw HttpParseError.malformed }
    lines.removeFirst()

    let parts = requestLine.split(separator: " ")
    guard parts.count == 3, parts[2].hasPrefix("HTTP/") else { throw HttpParseError.malformed }

    let target = String(parts[1])
    let split = target.split(separator: "?", maxSplits: 1, omittingEmptySubsequences: false)
    let path = String(split[0])

    var query: [String: String] = [:]
    if split.count == 2 {
        for pair in split[1].split(separator: "&") {
            let kv = pair.split(separator: "=", maxSplits: 1)
            guard let key = kv.first else { continue }
            let value = kv.count == 2 ? String(kv[1]) : ""
            query[String(key)] = value.removingPercentEncoding ?? value
        }
    }

    var headers: [String: String] = [:]
    for line in lines where !line.isEmpty {
        let kv = line.split(separator: ":", maxSplits: 1)
        guard kv.count == 2 else { throw HttpParseError.malformed }
        headers[kv[0].lowercased()] = kv[1].trimmingCharacters(in: .whitespaces)
    }

    let body = data[separator.upperBound...]
    let declared = Int(headers["content-length"] ?? "0") ?? 0
    guard body.count >= declared else { throw HttpParseError.incomplete }

    return HttpRequest(method: String(parts[0]).uppercased(), path: path, query: query,
                       headers: headers, body: Data(body.prefix(declared)))
}

public func parseHttpResponse(_ data: Data) throws -> (status: Int, headers: [String: String], body: Data) {
    guard let separator = data.range(of: crlfcrlf) else { throw HttpParseError.incomplete }

    let headText = String(decoding: data[data.startIndex..<separator.lowerBound], as: UTF8.self)
    var lines = headText.components(separatedBy: "\r\n")
    guard let statusLine = lines.first else { throw HttpParseError.malformed }
    lines.removeFirst()

    let parts = statusLine.split(separator: " ")
    guard parts.count >= 2, let status = Int(parts[1]) else { throw HttpParseError.malformed }

    var headers: [String: String] = [:]
    for line in lines where !line.isEmpty {
        let kv = line.split(separator: ":", maxSplits: 1)
        guard kv.count == 2 else { continue }
        headers[kv[0].lowercased()] = kv[1].trimmingCharacters(in: .whitespaces)
    }

    let body = Data(data[separator.upperBound...])
    if let declared = Int(headers["content-length"] ?? ""), body.count < declared {
        throw HttpParseError.incomplete
    }
    return (status, headers, body)
}
```

- [ ] **Step 5: Реализовать проверку приватности адреса**

`mac/Sources/LanClipCore/Net.swift`:

```swift
import Foundation

public func isPrivateAddress(_ address: String) -> Bool {
    let bare = address.split(separator: "%").first.map(String.init) ?? address
    if bare.isEmpty { return false }

    if bare.contains(":") {
        let lowered = bare.lowercased()
        return lowered == "::1" || lowered.hasPrefix("fe80:") || lowered.hasPrefix("fd")
    }

    let octets = bare.split(separator: ".").compactMap { Int($0) }
    guard octets.count == 4, octets.allSatisfy({ (0...255).contains($0) }) else { return false }

    if octets[0] == 127 || octets[0] == 10 { return true }
    if octets[0] == 192 && octets[1] == 168 { return true }
    if octets[0] == 172 && (16...31).contains(octets[1]) { return true }
    if octets[0] == 169 && octets[1] == 254 { return true }
    return false
}
```

- [ ] **Step 6: Проверить, что тесты проходят**

Run: `cd mac && swift test --filter HttpMessageTests && swift test --filter NetTests`
Expected: PASS, 8 и 2 теста.

- [ ] **Step 7: Коммит**

```bash
git add mac/Sources/LanClipCore/HttpMessage.swift mac/Sources/LanClipCore/Net.swift \
        mac/Tests/LanClipCoreTests/HttpMessageTests.swift mac/Tests/LanClipCoreTests/NetTests.swift
git commit -m "feat(mac): минимальный HTTP/1.1 и проверка приватности адреса"
```

---

Задачи 7–26 продолжают ту же схему и описаны ниже с той же степенью детализации.

### Task 7: Mac — HTTP-сервер на NWListener

**Files:**
- Create: `mac/Sources/LanClipCore/HttpServer.swift`
- Test: `mac/Tests/LanClipCoreTests/HttpServerTests.swift`

**Interfaces:**
- Consumes: `Config`, `SnapshotStore`, `SnapshotError`, `Manifest`, `HttpRequest`, `HttpResponse`, `parseHttpRequest`, `isPrivateAddress`, `protocolVersion`.
- Produces:

```swift
public final class HttpServer {
    public init(config: Config, snapshots: SnapshotStore, hostName: String,
                pull: @escaping () throws -> PullResultLike)
    public func start() throws          // при config.port == 0 занимает свободный порт
    public func stop()
    public var boundPort: Int { get }
    static func route(_ request: HttpRequest, config: Config, snapshots: SnapshotStore,
                      hostName: String, remote: String,
                      pull: () throws -> PullResultLike) -> HttpResponse
}

public struct PullResultLike: Codable, Equatable, Sendable {
    public let kind: ClipKind
    public let fileCount: Int
    public let bytes: Int
}
```

Маршрутизация вынесена в статический `route`, чтобы тесты проверяли все коды ответов без поднятия сокета; отдельные тесты поднимают реальный listener на `port: 0` и проверяют сквозной обмен.

Ответы: `403` если `!isPrivateAddress(remote)`; `401` если токен не совпал (сравнение по всей длине, без раннего выхода); `404` на неизвестный путь; `405` на неверный метод; `409` на `SnapshotError.staleSeq`; `503` с телом `{"error":…}` если `pull` бросил.

- [ ] **Step 1: Написать тесты маршрутизации** — по одному на каждый код ответа выше, плюс `GET /health` возвращает `{"ok":true,"host":…,"version":1}`, `GET /clip` возвращает манифест из `SnapshotStore`, `GET /clip/blob/0?seq=<актуальный>` возвращает байты с `Content-Type: application/octet-stream`, `POST /pull` возвращает `{"kind":…,"fileCount":…,"bytes":…}`.
- [ ] **Step 2: Написать тест сквозного обмена** — поднять `HttpServer` с `port: 0` и `FakeClipboard`, подключиться `NWConnection` к `127.0.0.1:boundPort`, послать `GET /clip` с токеном, проверить манифест; послать без токена, проверить `401`.
- [ ] **Step 3: Убедиться, что тесты падают** — `cd mac && swift test --filter HttpServerTests`, ожидаемо `cannot find 'HttpServer' in scope`.
- [ ] **Step 4: Реализовать `route`** — чистая функция над `HttpRequest`, без сети.
- [ ] **Step 5: Реализовать транспорт** — `NWListener` с `NWParameters.tcp`, на каждое соединение накапливать байты до успешного `parseHttpRequest`, отвечать `head()` плюс тело чанками по 256 КБ, закрывать соединение. Ограничить накопление 1 МБ и отвечать `400` при превышении, чтобы соединение нельзя было раздуть.
- [ ] **Step 6: Проверить, что тесты проходят** — `cd mac && swift test --filter HttpServerTests`.
- [ ] **Step 7: Коммит** — `feat(mac): HTTP-сервер буфера с авторизацией по токену`.

### Task 8: Mac — HTTP-клиент на NWConnection

**Files:**
- Create: `mac/Sources/LanClipCore/HttpClient.swift`
- Test: `mac/Tests/LanClipCoreTests/HttpClientTests.swift`

**Interfaces:**
- Consumes: `HttpResponse`, `parseHttpResponse`, `Manifest`.
- Produces:

```swift
public protocol HealthProbing: Sendable {
    func probe(host: String, port: Int, token: String, timeout: TimeInterval) -> Bool
}

public protocol BlobFetching: Sendable {
    func manifest(host: String, port: Int, token: String) throws -> Manifest
    func blob(host: String, port: Int, token: String, index: Int, seq: Int, to file: URL?) throws -> Data?
}

public enum HttpClientError: Error, Equatable {
    case timeout
    case status(Int)
    case transport(String)
}

public final class NwHttpClient: HealthProbing, BlobFetching {
    public init(timeout: TimeInterval = 10)
}
```

`blob(to:)` с непустым `file` пишет тело потоком в файл и возвращает `nil`; с `nil` возвращает байты в памяти. Статус `409` превращается в `HttpClientError.status(409)` — вызывающий трактует это как «буфер уехал».

- [ ] **Step 1: Написать тесты против реального сервера** — поднять `HttpServer` из задачи 7 на `port: 0` с `FakeClipboard`, проверить: `probe` истинен с верным токеном и ложен с неверным; `manifest` возвращает текстовый манифест; `blob(to: nil)` отдаёт PNG-байты; `blob(to: файл)` пишет их на диск; устаревший `seq` даёт `.status(409)`; недостижимый порт даёт `.transport`.
- [ ] **Step 2: Убедиться, что тесты падают.**
- [ ] **Step 3: Реализовать клиент** — `NWConnection` с ручной сборкой запроса, чтение до `parseHttpResponse` без `.incomplete`, таймаут через `DispatchSemaphore.wait(timeout:)`, потоковая запись в `FileHandle` для файлового варианта.
- [ ] **Step 4: Проверить, что тесты проходят.**
- [ ] **Step 5: Коммит** — `feat(mac): HTTP-клиент на NWConnection без ATS-ограничений`.

### Task 9: Mac — стейджинг принятых файлов и уборка

**Files:**
- Create: `mac/Sources/LanClipCore/Staging.swift`
- Test: `mac/Tests/LanClipCoreTests/StagingTests.swift`

**Interfaces:**
- Consumes: `RelPath`.
- Produces:

```swift
public struct StagingBatch {
    public let root: URL
    public func destination(for rel: String) throws -> URL   // создаёт промежуточные папки
}

public final class Staging {
    public static let keepBatches = 20
    public static let maxAgeDays = 7
    public init(root: URL, now: @escaping () -> Date = Date.init, fileManager: FileManager = .default)
    public func newBatch() throws -> StagingBatch
    public func cleanup() throws
    public static func stamp(_ date: Date) -> String     // yyyyMMdd-HHmmss
    public static var defaultRoot: URL { get }           // ~/Library/Application Support/lanclip/incoming
}

public enum StagingError: Error, Equatable { case unsafeRelativePath(String) }
```

- [ ] **Step 1: Написать тесты** — `stamp` даёт формат `yyyyMMdd-HHmmss` для фиксированной даты; `newBatch` создаёт папку с меткой; `destination(for:)` создаёт вложенные папки и не выходит за `root`; небезопасный `rel` (`../x`) бросает `unsafeRelativePath`; `cleanup` удаляет партии старше 7 дней при подставном `now` и оставляет ровно 20 последних при 25 созданных.
- [ ] **Step 2: Убедиться, что тесты падают.**
- [ ] **Step 3: Реализовать стейджинг** — метка через `DateFormatter` с `posix`-локалью; при коллизии метки добавлять суффикс `-2`, `-3`; в `destination` прогонять `rel` через `RelPath.normalize` и дополнительно проверять, что итоговый `standardized.path` начинается с `root.standardized.path`.
- [ ] **Step 4: Проверить, что тесты проходят.**
- [ ] **Step 5: Коммит** — `feat(mac): партии принятых файлов и уборка стейджинга`.

### Task 10: Mac — резолв соседа

**Files:**
- Create: `mac/Sources/LanClipCore/PeerResolver.swift`
- Test: `mac/Tests/LanClipCoreTests/PeerResolverTests.swift`

**Interfaces:**
- Consumes: `Config`, `HealthProbing`.
- Produces:

```swift
public final class PeerResolver {
    public init(config: Config, prober: HealthProbing, timeout: TimeInterval = 2)
    public func resolve() -> String?     // кешированный адрес либо перебор peers по порядку
    public func invalidate()
}
```

- [ ] **Step 1: Написать тесты** с подставным `HealthProbing`, считающим вызовы: возвращается первый живой адрес; мёртвые адреса пропускаются; повторный `resolve()` не опрашивает сеть (счётчик не растёт); после `invalidate()` опрос повторяется; если живых нет — `nil`; если бывший живой умер, а следующий жив, после `invalidate()` возвращается следующий.
- [ ] **Step 2: Убедиться, что тесты падают.**
- [ ] **Step 3: Реализовать резолвер** с кешем в приватном поле под `NSLock`.
- [ ] **Step 4: Проверить, что тесты проходят.**
- [ ] **Step 5: Коммит** — `feat(mac): резолв соседа с кешем живого адреса`.

### Task 11: Mac — полный цикл pull

**Files:**
- Create: `mac/Sources/LanClipCore/PullClient.swift`
- Test: `mac/Tests/LanClipCoreTests/PullClientTests.swift`

**Interfaces:**
- Consumes: `Config`, `PeerResolver`, `BlobFetching`, `Staging`, `ClipboardWriting`, `Manifest`, `PullResultLike`.
- Produces:

```swift
public enum PullError: Error, Equatable {
    case noPeer
    case peerEmpty
    case tooLarge(totalSize: Int, maxBytes: Int)
    case changedMidTransfer
    case transport(String)
}

public final class PullClient {
    public init(config: Config, resolver: PeerResolver, fetcher: BlobFetching,
                staging: Staging, writer: ClipboardWriting)
    public func pull() throws -> PullResultLike
}
```

Порядок работы: `resolve()` или `noPeer`; `manifest`; `kind == .empty` → `peerEmpty`; `totalSize > maxBytes` → `tooLarge`; загрузка (картинка в память, файлы — в партию стейджинга); `409` от клиента → `changedMidTransfer` без записи в буфер; запись через `ClipboardWriting`; `staging.cleanup()`; результат. Любая ошибка транспорта вызывает `resolver.invalidate()`.

- [ ] **Step 1: Написать тесты** с подставными `BlobFetching`/`HealthProbing` и `FakeClipboard`: текст пишется в буфер и даёт `kind: .text`; картинка приходит как `.image` с PNG-байтами; два файла ложатся в партию с сохранением `rel` и в буфер уходят локальные пути; превышение `maxBytes` даёт `tooLarge` и буфер не тронут; `409` даёт `changedMidTransfer` и буфер не тронут; пустой буфер соседа даёт `peerEmpty`; отсутствие живого соседа даёт `noPeer`; ошибка транспорта инвалидирует кеш резолвера.
- [ ] **Step 2: Написать сквозной тест «ядро против ядра»** — поднять `HttpServer` на `port: 0` с одним `FakeClipboard` и выполнить `PullClient` с `NwHttpClient` во второй `FakeClipboard`; проверить перенос текста, картинки и двух файлов.
- [ ] **Step 3: Убедиться, что тесты падают.**
- [ ] **Step 4: Реализовать `PullClient`.**
- [ ] **Step 5: Проверить, что тесты проходят.**
- [ ] **Step 6: Коммит** — `feat(mac): полный цикл pull с лимитом размера и защитой от смены буфера`.

### Task 12: Mac — NSPasteboard

Первая платформенная задача: тесты работают с настоящим системным буфером, поэтому запускаются только в GUI-сессии пользователя (не по SSH).

**Files:**
- Create: `mac/Sources/lanclipd/MacPasteboard.swift`
- Create: `mac/Sources/LanClipCore/Notifier.swift`
- Test: `mac/Tests/LanClipCoreTests/MacPasteboardTests.swift` (тестируемый тип переносится в `LanClipCore`, если тесту нужен доступ; иначе тест живёт в отдельной цели `lanclipdTests`)

**Interfaces:**
- Consumes: `ClipboardReading`, `ClipboardWriting`, `ClipContent`.
- Produces:

```swift
public final class MacPasteboard: ClipboardReading, ClipboardWriting {
    public init(pasteboard: NSPasteboard = .general)
    public func changeCount() -> Int
    public func read() throws -> ClipContent
    public func write(_ content: ClipContent) throws
}

public protocol Notifying: Sendable { func error(_ message: String); func info(_ message: String) }
public struct NullNotifier: Notifying, Sendable { public init() }
```

Чтение: сначала `readObjects(forClasses: [NSURL.self], options: [.urlReadingFileURLsOnly: true])` — если непусто, это `.files`; затем `data(forType: .png)`, а при отсутствии — `.tiff` с перекодированием в PNG через `NSBitmapImageRep`; затем `string(forType: .string)`; иначе `.empty`. Запись файлов объявляет и `NSURL`-объекты, и легаси `NSFilenamesPboardType`, чтобы старые приложения тоже видели файлы.

- [ ] **Step 1: Написать тесты на отдельном `NSPasteboard(name:)`** (не системном, чтобы не портить буфер Павла): запись и чтение текста с кириллицей; запись PNG и чтение обратно как `.image`; запись двух файлов и чтение как `.files` с теми же путями; `changeCount` растёт после записи; пустой буфер даёт `.empty`; TIFF в буфере читается как PNG.
- [ ] **Step 2: Убедиться, что тесты падают.**
- [ ] **Step 3: Реализовать `MacPasteboard` и `Notifier`.**
- [ ] **Step 4: Проверить, что тесты проходят** — `cd mac && swift test --filter MacPasteboardTests`, запускать в локальной GUI-сессии.
- [ ] **Step 5: Коммит** — `feat(mac): чтение и запись NSPasteboard для текста, картинок и файлов`.

### Task 13: Mac — CLI serve/status/get/pull

**Files:**
- Modify: `mac/Sources/lanclipd/main.swift`
- Create: `mac/Sources/lanclipd/MacNotifier.swift`
- Test: `mac/Tests/LanClipCoreTests/CliArgsTests.swift`

**Interfaces:**
- Consumes: всё ядро плюс `MacPasteboard`.
- Produces: `public struct CliArgs { public let command: String; public let configURL: URL; public static func parse(_ argv: [String]) throws -> CliArgs }` в `LanClipCore`, а также поведение подкоманд:
  - `serve` — валидирует конфиг, поднимает `HttpServer`, регистрирует хоткей (задача 14), крутит `RunLoop`;
  - `status` — печатает свой порт, `seq`, `kind` своего буфера и адрес живого соседа либо «сосед не найден»;
  - `get` — печатает манифест соседа в JSON, ничего не меняя локально;
  - `pull` — выполняет `PullClient.pull()` и печатает результат;
  - неизвестная команда — сообщение об использовании и код выхода 2.

- [ ] **Step 1: Написать тесты `CliArgs.parse`** — команда по умолчанию `serve`; `--config <путь>` учитывается; неизвестная команда бросает ошибку; `-h`/`--help` распознаётся.
- [ ] **Step 2: Убедиться, что тесты падают.**
- [ ] **Step 3: Реализовать `CliArgs` и обвязку в `main.swift`** — все ошибки конфига печатать по-русски с путём к файлу и подсказкой, что впиcать в `peers`.
- [ ] **Step 4: Проверить руками** — `swift run lanclipd status` печатает состояние; `swift run lanclipd get` при выключенном соседе печатает «сосед не найден» и выходит с кодом 1.
- [ ] **Step 5: Коммит** — `feat(mac): CLI агента с подкомандами serve/status/get/pull`.

### Task 14: Mac — хоткей и синтез паста

**Files:**
- Create: `mac/Sources/lanclipd/MacHotkey.swift`
- Modify: `mac/Sources/lanclipd/main.swift`

**Interfaces:**
- Consumes: `Config`, `PullClient`, `Notifying`.
- Produces:

```swift
final class MacHotkey {
    init(onPress: @escaping () -> Void)
    func register() throws        // Ctrl+Shift+V через RegisterEventHotKey
    func unregister()
}

func synthesizePaste()            // очищает физические модификаторы и посылает Cmd+V
```

- [ ] **Step 1: Реализовать регистрацию хоткея** — `RegisterEventHotKey(UInt32(kVK_ANSI_V), UInt32(controlKey | shiftKey), …, GetApplicationEventTarget(), 0, &ref)` плюс `InstallEventHandler` на `kEventHotKeyPressed`.
- [ ] **Step 2: Реализовать синтез паста** — источник `CGEventSource(stateID: .combinedSessionState)`; сначала keyUp для левых и правых Ctrl (`0x3B`, `0x3E`) и Shift (`0x38`, `0x3C`), затем пауза 20 мс, затем keyDown/keyUp для `V` (`0x09`) с `flags = .maskCommand`. Без сброса модификаторов приложение получит Cmd+Ctrl+Shift+V и проигнорирует его.
- [ ] **Step 3: Связать с pull** — обработчик выполняет `PullClient.pull()` на фоновой очереди, при `autoPaste` вызывает `synthesizePaste()` в главной очереди, на `PullError` показывает уведомление, при `kind == .files` — тост «N файлов, M МБ».
- [ ] **Step 4: Проверить руками** — запустить `swift run lanclipd serve` из Терминала, выдать Accessibility и Local Network по появившимся диалогам, скопировать текст на ПК (агент ПК появится в задаче 24 — до этого проверка делается против второго локального экземпляра на другом порту), нажать Ctrl+Shift+V в TextEdit.
- [ ] **Step 5: Коммит** — `feat(mac): глобальный Ctrl+Shift+V и синтез паста с сбросом модификаторов`.

### Task 15: Mac — установка и автозапуск

**Files:**
- Create: `scripts/install-mac.sh`
- Create: `scripts/gen-token.sh`

- [ ] **Step 1: Написать gen-token.sh** — печатает 32 hex-символа из `/dev/urandom`, чтобы одним и тем же токеном заполнить оба конфига.
- [ ] **Step 2: Написать install-mac.sh** — `swift build -c release`; копирование бинарника в `~/.local/bin/lanclipd` (стабильный путь важен: разрешение Accessibility привязано к пути); `codesign -s - --force ~/.local/bin/lanclipd`, чтобы пересборка не сбрасывала разрешение; создание конфига, если его нет; запись `~/Library/LaunchAgents/space.paulislava.lanclip.plist` с `KeepAlive`, `RunAtLoad`, `StandardOutPath`/`StandardErrorPath` в `~/Library/Logs/lanclip.log`; `launchctl bootout` и `bootstrap gui/$(id -u)`.
- [ ] **Step 3: Проверить** — `launchctl print gui/$(id -u)/space.paulislava.lanclip` показывает запущенный процесс; `~/.local/bin/lanclipd status` отвечает; в логе нет ошибок.
- [ ] **Step 4: Зафиксировать в документации**, что первый запуск делается из Терминала руками — фоновому LaunchAgent диалог Local Network может не показаться.
- [ ] **Step 5: Коммит** — `feat(mac): установка агента, ad-hoc подпись и LaunchAgent`.

### Task 16: Win — ядро моделей (JSON, манифест, относительные пути)

**Files:**
- Create: `win/src/Json.cs`, `win/src/Manifest.cs`, `win/src/RelPath.cs`
- Test: `win/tests/RelPathTests.cs`, `win/tests/ManifestTests.cs`

**Interfaces:**
- Consumes: `LanClip.Version`.
- Produces:

```csharp
static class Json {
    public static string Write(object value);                       // JavaScriptSerializer, MaxJsonLength = int.MaxValue
    public static Dictionary<string, object> Parse(string text);    // бросает FormatException на мусор
    public static string Str(Dictionary<string, object> o, string key, string fallback);
    public static int Int(Dictionary<string, object> o, string key, int fallback);
    public static bool Bool(Dictionary<string, object> o, string key, bool fallback);
    public static List<object> Arr(Dictionary<string, object> o, string key);
}

class BlobRef { public int I; public string Rel; public long Size; public string Mime; }

class Manifest {
    public string Kind; public int Seq; public string Text;
    public List<BlobRef> Blobs; public long TotalSize;
    public static Manifest Empty(int seq);
    public static Manifest OfText(string value, int seq);
    public static Manifest OfImage(long pngSize, int seq);
    public static Manifest OfFiles(List<BlobRef> blobs, int seq);
    public string ToJson();
    public static Manifest FromJson(string text);
}

static class RelPath { public static string Normalize(string raw); }   // null, если путь неприемлем
```

`RelPath.Normalize` реализует те же семь правил, что и Swift-версия из задачи 2, и обязан давать те же результаты на тех же входах — это часть контракта между сторонами.

- [ ] **Step 1: Написать тесты `RelPath`** — перенести все случаи из задачи 2 один в один, включая кириллицу, `NUL` → `NUL_`, `C:\Users\a?b.txt` → `C_/Users/a_b.txt`, отказ на `../secret` и на слишком длинных путях.
- [ ] **Step 2: Написать тесты манифеста** — `Empty` не содержит ключей `text`/`blobs`/`totalSize` в JSON; `OfText` содержит только `text`; `OfImage` даёт единственный блоб `clip.png` с `image/png`; `OfFiles` суммирует `totalSize`; round-trip через JSON; разбор манифеста, пришедшего от Swift-агента (строка с `\u`-экранированной кириллицей); отказ на неизвестном `kind`.
- [ ] **Step 3: Убедиться, что тесты падают** — `./scripts/sync-win.sh`.
- [ ] **Step 4: Реализовать `Json`, `RelPath`, `Manifest`** на C# 5: только конкатенация строк, никаких `?.` и `=>`.
- [ ] **Step 5: Проверить, что тесты проходят** — `./scripts/sync-win.sh`, `passed=… failed=0`.
- [ ] **Step 6: Коммит** — `feat(win): ядро моделей — JSON, манифест и нормализация путей`.

### Task 17: Win — конфиг

**Files:**
- Create: `win/src/Config.cs`
- Test: `win/tests/ConfigTests.cs`

**Interfaces:**
- Produces: `class Config { public int Port; public string Token; public List<string> Peers; public long MaxBytes; public bool AutoPaste; public static string DefaultPath(); public static Config Load(string path); public void Save(string path); public void Validate(); public static string GenerateToken(); }` и `class ConfigException : Exception`.

`DefaultPath()` — `%USERPROFILE%\.config\lanclip\config.json`. `Save` пишет UTF-8 **без BOM** через `new UTF8Encoding(false)`: BOM ломает разбор конфига на другой стороне.

- [ ] **Step 1: Написать тесты** — зеркало задачи 4: создание файла при отсутствии, значения по умолчанию для отсутствующих ключей, явные значения, отказ на мусорном JSON, `Validate` на каждом плохом поле, отсутствие BOM в записанном файле.
- [ ] **Step 2: Убедиться, что тесты падают.**
- [ ] **Step 3: Реализовать `Config`.**
- [ ] **Step 4: Проверить, что тесты проходят.**
- [ ] **Step 5: Коммит** — `feat(win): конфиг агента с записью UTF-8 без BOM`.

### Task 18: Win — абстракция буфера, снимок и STA-исполнитель

**Files:**
- Create: `win/src/IClipboard.cs`, `win/src/Snapshot.cs`, `win/src/StaExecutor.cs`
- Test: `win/tests/SnapshotTests.cs`, `win/tests/FakeClipboard.cs`

**Interfaces:**
- Produces:

```csharp
enum ClipKindValue { Empty, Text, Image, Files }

class ClipContent {
    public ClipKindValue Kind; public string Text; public byte[] Png; public List<string> Files;
    public static ClipContent Empty(); public static ClipContent OfText(string s);
    public static ClipContent OfImage(byte[] png); public static ClipContent OfFiles(List<string> paths);
}

interface IClipboardReader { int ChangeCount(); ClipContent Read(); }
interface IClipboardWriter { void Write(ClipContent content); }

class ClipSnapshot { public Manifest Manifest; public byte[] ImagePng; public Dictionary<int, string> Sources; }

class SnapshotStore {
    public SnapshotStore(IClipboardReader reader);
    public ClipSnapshot Current();
    public byte[] Blob(int index, int seq);      // null — индекс вне диапазона
}
class StaleSeqException : Exception { }

class StaExecutor {           // единственный STA-поток, на который маршалятся все операции с буфером
    public StaExecutor();
    public TR Invoke<TR>(Func<TR> body);
    public void Invoke(Action body);
    public void Shutdown();
}
```

`StaExecutor` обязателен: коллбэки `HttpListener` приходят в MTA-потоках, а `System.Windows.Forms.Clipboard` требует STA. Без него доступ к буферу будет падать через раз.

- [ ] **Step 1: Написать `FakeClipboard`** — зеркало Swift-версии, со счётчиком изменений и журналом записей.
- [ ] **Step 2: Написать тесты снимка** — зеркало задачи 5: пустой буфер, текст с `seq` из счётчика, PNG-блоб, одиночный файл с голым именем, рекурсивный обход папки, `StaleSeqException` на устаревшем `seq`, `null` на индексе вне диапазона.
- [ ] **Step 3: Написать тесты `StaExecutor`** — `Invoke` возвращает значение; поток исполнения имеет `ApartmentState.STA`; исключение из тела прокидывается вызывающему; после `Shutdown` повторный `Invoke` бросает.
- [ ] **Step 4: Убедиться, что тесты падают.**
- [ ] **Step 5: Реализовать всё три файла.**
- [ ] **Step 6: Проверить, что тесты проходят.**
- [ ] **Step 7: Коммит** — `feat(win): абстракция буфера, снимок манифеста и STA-исполнитель`.

### Task 19: Win — HTTP-сервер на HttpListener

**Files:**
- Create: `win/src/HttpServer.cs`
- Test: `win/tests/HttpServerTests.cs`

**Interfaces:**
- Produces: `class HttpServer { public HttpServer(Config config, SnapshotStore snapshots, string hostName, Func<PullResult> pull); public void Start(); public void Stop(); public int BoundPort; }` и `static HttpResponseSpec Route(...)` для тестов без сокета, где `class HttpResponseSpec { public int Status; public string ContentType; public byte[] Body; }`.

Коды ответов — те же, что на Mac: `403` на неприватный адрес, `401` на неверный токен, `404`, `405`, `409` на устаревший `seq`, `503` с `{"error":…}` на сорванный pull.

- [ ] **Step 1: Написать тесты маршрутизации** — по одному на каждый код, плюс содержимое `/health` (`version` равен 1) и `/clip`.
- [ ] **Step 2: Написать тест сквозного обмена** — поднять сервер на `http://127.0.0.1:<свободный порт>/` (для localhost-префикса в тестах urlacl не нужен, сессия сборки уже административная), сходить `HttpWebRequest`, проверить манифест и `401` без токена.
- [ ] **Step 3: Убедиться, что тесты падают.**
- [ ] **Step 4: Реализовать сервер** — префикс `http://+:<port>/` в рабочем режиме, все обращения к буферу через `StaExecutor`, тело блоба отдавать потоком по 256 КБ.
- [ ] **Step 5: Проверить, что тесты проходят.**
- [ ] **Step 6: Коммит** — `feat(win): HTTP-сервер буфера на HttpListener`.

### Task 20: Win — стейджинг и уборка

**Files:**
- Create: `win/src/Staging.cs`
- Test: `win/tests/StagingTests.cs`

**Interfaces:**
- Produces: `class Staging { public const int KeepBatches = 20; public const int MaxAgeDays = 7; public Staging(string root, Func<DateTime> now); public StagingBatch NewBatch(); public void Cleanup(); public static string Stamp(DateTime when); public static string DefaultRoot(); }` и `class StagingBatch { public string Root; public string Destination(string rel); }`.

`DefaultRoot()` — `%LOCALAPPDATA%\lanclip\incoming`.

- [ ] **Step 1: Написать тесты** — зеркало задачи 9, включая отказ на `../x` и удержание 20 последних партий из 25.
- [ ] **Step 2: Убедиться, что тесты падают.**
- [ ] **Step 3: Реализовать стейджинг** — проверять, что итоговый `Path.GetFullPath` начинается с корня.
- [ ] **Step 4: Проверить, что тесты проходят.**
- [ ] **Step 5: Коммит** — `feat(win): партии принятых файлов и уборка стейджинга`.

### Task 21: Win — резолв соседа и полный цикл pull

**Files:**
- Create: `win/src/HttpClient.cs`, `win/src/PeerResolver.cs`, `win/src/PullClient.cs`
- Test: `win/tests/PeerResolverTests.cs`, `win/tests/PullClientTests.cs`

**Interfaces:**
- Produces:

```csharp
interface IHealthProber { bool Probe(string host, int port, string token, int timeoutMs); }
interface IBlobFetcher {
    Manifest Manifest(string host, int port, string token);
    byte[] Blob(string host, int port, string token, int index, int seq, string toFile);
}
class WebBlobFetcher : IHealthProber, IBlobFetcher { public WebBlobFetcher(int timeoutMs); }

class PeerResolver { public PeerResolver(Config config, IHealthProber prober); public string Resolve(); public void Invalidate(); }

class PullResult { public string Kind; public int FileCount; public long Bytes; public string ToJson(); }
class PullException : Exception { public string Code; }   // noPeer | peerEmpty | tooLarge | changed | transport

class PullClient {
    public PullClient(Config config, PeerResolver resolver, IBlobFetcher fetcher,
                      Staging staging, IClipboardWriter writer);
    public PullResult Pull();
}
```

- [ ] **Step 1: Написать тесты резолвера** — зеркало задачи 10.
- [ ] **Step 2: Написать тесты pull** — зеркало задачи 11, включая `tooLarge`, `changed` (сервер вернул 409) и «буфер не тронут при ошибке».
- [ ] **Step 3: Написать сквозной тест «ядро против ядра»** — поднять локальный `HttpServer` с одним `FakeClipboard` и выполнить `PullClient` во второй.
- [ ] **Step 4: Убедиться, что тесты падают.**
- [ ] **Step 5: Реализовать клиент, резолвер и pull** на `HttpWebRequest` со `Timeout` и потоковой записью в файл.
- [ ] **Step 6: Проверить, что тесты проходят.**
- [ ] **Step 7: Коммит** — `feat(win): резолв соседа и полный цикл pull`.

### Task 22: Win — системный буфер

**Files:**
- Create: `win/src/WinClipboard.cs`
- Test: `win/tests/WinClipboardTests.cs`

**Interfaces:**
- Produces: `class WinClipboard : IClipboardReader, IClipboardWriter { public WinClipboard(StaExecutor sta); public int ChangeCount(); public ClipContent Read(); public void Write(ClipContent content); }`

`ChangeCount` — P/Invoke `GetClipboardSequenceNumber` из `user32.dll`. Чтение: `FileDropList` → `.Files`; иначе `GetImage` с сохранением в PNG через `MemoryStream` и `ImageFormat.Png`; иначе `GetText`; иначе `Empty`. Запись файлов — `SetFileDropList` с `StringCollection`.

- [ ] **Step 1: Написать тесты** — текст с кириллицей туда и обратно; PNG туда и обратно; список файлов туда и обратно; `ChangeCount` растёт после записи; пустой буфер даёт `Empty`. Тесты трогают настоящий буфер машины: в начале сохранить его содержимое и восстановить в конце.
- [ ] **Step 2: Убедиться, что тесты падают.**
- [ ] **Step 3: Реализовать `WinClipboard`** — каждая операция строго через `StaExecutor`.
- [ ] **Step 4: Проверить, что тесты проходят** — `./scripts/sync-win.sh`. Если тесты падают на доступе к буферу, значит SSH-сессия получила отдельную оконную станцию: тогда прогнать `lanclip-tests.exe` в интерактивной сессии через разовую `Register-ScheduledTask` с записью вывода в файл и зафиксировать это в `ai/ERRORS.md`.
- [ ] **Step 5: Коммит** — `feat(win): чтение и запись системного буфера через STA-поток`.

### Task 23: Win — CLI и уведомления

**Files:**
- Modify: `win/src/Program.cs`
- Create: `win/src/Notifier.cs`
- Test: `win/tests/CliArgsTests.cs`

**Interfaces:**
- Produces: `class CliArgs { public string Command; public string ConfigPath; public static CliArgs Parse(string[] argv); }`, `interface INotifier { void Error(string message); void Info(string message); }`, `class TrayNotifier : INotifier`, `class NullNotifier : INotifier`.

Подкоманды те же, что на Mac: `serve`, `status`, `get`, `pull`; неизвестная — использование и код выхода 2.

- [ ] **Step 1: Написать тесты `CliArgs.Parse`** — зеркало задачи 13.
- [ ] **Step 2: Убедиться, что тесты падают.**
- [ ] **Step 3: Реализовать CLI и `TrayNotifier`** на `NotifyIcon` с `ShowBalloonTip`.
- [ ] **Step 4: Проверить руками по SSH** — `lanclipd.exe status` печатает состояние, `lanclipd.exe get` при выключенном соседе печатает «сосед не найден» и выходит с кодом 1.
- [ ] **Step 5: Коммит** — `feat(win): CLI агента и уведомления в трее`.

### Task 24: Win — хоткей и синтез паста

**Files:**
- Create: `win/src/Hotkey.cs`
- Modify: `win/src/Program.cs`

**Interfaces:**
- Produces: `class Hotkey : IDisposable { public Hotkey(Action onPress); public void Register(); }` и `static class Paste { public static void Send(); }`.

`Register` создаёт скрытое окно с циклом сообщений и вызывает `RegisterHotKey(hWnd, 1, MOD_CONTROL | MOD_SHIFT, 0x56)`. `Paste.Send` через `SendInput`: сначала keyUp для `VK_CONTROL`, `VK_LCONTROL`, `VK_RCONTROL`, `VK_SHIFT`, `VK_LSHIFT`, `VK_RSHIFT`, затем пауза 20 мс, затем Ctrl down, `V` down, `V` up, Ctrl up. Без сброса физических модификаторов приложение получит Ctrl+Shift+V и вставит как plain text или проигнорирует.

- [ ] **Step 1: Реализовать хоткей и синтез паста.**
- [ ] **Step 2: Связать с pull** — как на Mac: pull в фоне, паст при `AutoPaste`, уведомление на ошибке, тост про файлы.
- [ ] **Step 3: Проверить руками** — запустить `lanclipd.exe serve` в интерактивной сессии, скопировать текст на Mac, нажать Ctrl+Shift+V в Блокноте.
- [ ] **Step 4: Коммит** — `feat(win): глобальный Ctrl+Shift+V и синтез паста через SendInput`.

### Task 25: Win — установка, права и автозапуск

**Files:**
- Create: `scripts/install-win.ps1`

- [ ] **Step 1: Написать скрипт** — сборка через `build.ps1`; копирование `out\lanclipd.exe` в `C:\Users\PaulIsLava\.local\bin\lanclipd.exe`; создание конфига, если его нет (UTF-8 без BOM); `netsh http add urlacl url=http://+:8899/ user=DESKTOP-NGNE28E\paulislava`; `New-NetFirewallRule -DisplayName "lanclip" -Direction Inbound -LocalPort 8899 -Protocol TCP -Action Allow -Profile Private`; `Register-ScheduledTask` с триггером `-AtLogOn`, пользователем из `[Security.Principal.WindowsIdentity]::GetCurrent().Name` (не `$env:USERDOMAIN`) и «запускать только при входе пользователя».
- [ ] **Step 2: Сделать скрипт идемпотентным** — повторный запуск не должен падать на уже существующих urlacl, правиле файрвола и задаче.
- [ ] **Step 3: Проверить** — `Get-ScheduledTask lanclip` показывает задачу; после `Start-ScheduledTask` процесс живёт и отвечает на `/health` с Mac.
- [ ] **Step 4: Коммит** — `feat(win): установка агента, urlacl, правило файрвола и автозапуск`.

### Task 26: Кросс-машинная проверка по матрице из спеки

**Files:**
- Create: `scripts/smoke.sh`

- [ ] **Step 1: Раздать общий токен** — сгенерировать `scripts/gen-token.sh`, вписать один и тот же токен в оба конфига; в конфиг Mac `peers: ["pc", "192.168.1.184"]`, в конфиг ПК `peers: ["cab-wsm-0667615.local", "192.168.1.102"]`.
- [ ] **Step 2: Написать smoke.sh** — проверяет `/health` в обе стороны, затем прогоняет `lanclipd get` с Mac и `lanclipd.exe get` по SSH, печатая результат каждой проверки.
- [ ] **Step 3: Пройти матрицу вручную в обе стороны** — текст ASCII, текст с кириллицей и emoji, многострочный текст, скриншот, один файл, много файлов, папка с вложенностью, имена с пробелами и кириллицей.
- [ ] **Step 4: Пройти негативные случаи** — пустой буфер соседа, выключенный сосед, неверный токен, партия больше `maxBytes`. Каждый должен давать внятное уведомление, а не тишину.
- [ ] **Step 5: Записать всё найденное** в `ai/ERRORS.md` по ходу, а не после.
- [ ] **Step 6: Коммит** — `test: кросс-машинная проверка буфера по матрице содержимого`.

### Task 27: Документация

**Files:**
- Create: `ai/PROJECT.md`, `ai/FEATURES.md`, `ai/ERRORS.md`, `README.md`

- [ ] **Step 1: Написать `ai/PROJECT.md`** — назначение, топология, протокол, где что лежит, как собирать и устанавливать обе стороны, где логи.
- [ ] **Step 2: Написать `ai/FEATURES.md`** — реализованные возможности со ссылками на файлы; отдельный `ai/features/clipboard-pull.md` с деталями цикла pull и обработкой `seq`.
- [ ] **Step 3: Заполнить `ai/ERRORS.md`** — накопленное за реализацию: C# 5 без Roslyn, BOM от PowerShell, оконные станции и буфер по SSH, Accessibility и Local Network на macOS, urlacl для `HttpListener`, сброс модификаторов перед синтезом паста.
- [ ] **Step 4: Написать README.md** — короткая инструкция: установка на Mac, установка на ПК, обмен токеном, как проверить.
- [ ] **Step 5: Сохранить заметку в Obsidian** — `Features/lanclip/shared-clipboard.md` с фронтматтером `project: lanclip`, `date: 2026-08-18`, `tags: [features]`, разделами «Контекст», «Решение», «Файлы»; при недоступности MCP `obsidian` писать файл напрямую в `/Users/pkondratov/Yandex.Disk.localized/Projects`.
- [ ] **Step 6: Добавить строку в `~/Documents/work-duties.md`** — только если проект относится к `git.sberdevices.ru`; `lanclip` личный, поэтому строка не нужна.
- [ ] **Step 7: Коммит** — `docs: документация lanclip и запись накопленных грабель`.

---

## Self-Review

**Покрытие спеки.** Протокол — задачи 1, 3, 6, 7, 8, 16, 19, 21. Конфиг и адресация — 4, 17, 26. Стейджинг и уборка — 9, 20. Поток по хоткею — 14, 24. Три обязательных разрешения — 15 (Accessibility, Local Network), 25 (urlacl, файрвол). Зажатые модификаторы — 14, 24. Сессия рабочего стола — 15 (LaunchAgent), 25 (задача при входе). Таблица компонентов из спеки — задачи 2–14 на Mac и 16–24 на Windows, один к одному. Тестирование всех четырёх уровней — юнит в 2–6, 9, 10, 16–18, 20, протокол с подставным буфером в 7, 8, 11, 19, 21, кросс-машинная интеграция в 26, ручная матрица в 26. Вне объёма спеки ничего не добавлено.

**Плейсхолдеры.** Задачи 1–6 содержат полный код тестов и реализаций. Задачи 7–27 задают точные сигнатуры в блоках Interfaces, перечисляют конкретные тестовые случаи и конкретные коды ответов, а не «добавить обработку ошибок»; повторяющиеся зеркальные реализации ссылаются на уже приведённый в задачах 2–11 код как на образец поведения, но каждая имеет свой список проверок.

**Согласованность типов.** `Manifest`/`Manifest`, `BlobRef`/`BlobRef`, `ClipContent`/`ClipContent`, `SnapshotStore.blob(index:seq:)`/`SnapshotStore.Blob(index, seq)`, `PullError`/`PullException.Code`, `HealthProbing`/`IHealthProber`, `BlobFetching`/`IBlobFetcher`, `Staging.keepBatches`/`Staging.KeepBatches` — имена парны между платформами и совпадают с тем, что объявлено в блоках Interfaces. `PullResultLike` объявлен в задаче 7 и потребляется задачами 11 и 13; `Notifying` объявлен в задаче 12 и потребляется задачами 13 и 14.
